# Фаза 3: UI/UX Дизайнер — Результаты

> **Стилистический ориентир: Duolingo**
> Яркие цвета, округлые формы, 3D-кнопки с нижней тенью, геймификация (стрики, прогресс-бары, праздничные анимации), дружелюбный тон, крупные touch targets, чистые экраны без визуального шума.

---

## 1. Дизайн-система

### 1.1 Цветовая палитра

Палитра вдохновлена Duolingo: насыщенный зелёный как primary, яркие акцентные цвета для геймификации, чистый светлый фон.

```typescript
// theme/colors.ts
export const colors = {
  light: {
    // Бренд
    primary: '#58CC02',           // Duolingo-green — основной цвет, успех, CTA
    primaryDark: '#46A302',       // Нижняя тень 3D-кнопок (primary)
    primaryLight: '#89E219',      // Hover / highlight

    // Акцентные (геймификация)
    blue: '#1CB0F6',              // Информация, ссылки, выбранные элементы
    blueDark: '#1899D6',          // Нижняя тень (blue)
    orange: '#FF9600',            // Стрик, огонь, предупреждение
    orangeDark: '#E08600',        // Нижняя тень (orange)
    red: '#FF4B4B',               // Ошибка, неправильный ответ
    redDark: '#EA2B2B',           // Нижняя тень (red)
    purple: '#CE82FF',            // Бонус, премиум, награда
    purpleDark: '#B666E5',        // Нижняя тень (purple)
    gold: '#FFC800',              // Очки, достижения, звёзды
    goldDark: '#E5B400',

    // Нейтральные
    background: '#F7F7F7',        // Фон приложения (светло-серый)
    surface: '#FFFFFF',           // Фон карточек
    surfaceVariant: '#F0F0F0',    // Альтернативный фон (input, disabled)
    border: '#E5E5E5',            // Границы карточек
    borderDark: '#CDCDCD',        // Нижняя тень карточек (3D-эффект)

    // Текст
    textPrimary: '#4B4B4B',       // Основной текст
    textSecondary: '#AFAFAF',     // Вторичный текст
    textOnPrimary: '#FFFFFF',     // Текст на primary
    textOnDark: '#FFFFFF',        // Текст на тёмном фоне

    // Системные
    success: '#58CC02',           // = primary
    warning: '#FF9600',           // = orange
    error: '#FF4B4B',             // = red
    info: '#1CB0F6',              // = blue

    // Специальные
    adContainer: '#F0F0F0',       // Фон рекламного блока
    streakFire: '#FF9600',        // Цвет огня стрика
    factCard: '#FFF9E6',          // Фон карточки факта (тёплый бежевый)
    factCardBorder: '#FFE066',    // Рамка карточки факта
    overlay: 'rgba(0,0,0,0.5)',   // Оверлей модалок
  },
  dark: {
    primary: '#58CC02',
    primaryDark: '#46A302',
    primaryLight: '#89E219',

    blue: '#1CB0F6',
    blueDark: '#1899D6',
    orange: '#FF9600',
    orangeDark: '#E08600',
    red: '#FF4B4B',
    redDark: '#EA2B2B',
    purple: '#CE82FF',
    purpleDark: '#B666E5',
    gold: '#FFC800',
    goldDark: '#E5B400',

    background: '#131F24',        // Тёмный фон (Duolingo dark)
    surface: '#1B2B32',           // Фон карточек
    surfaceVariant: '#243640',    // Альтернативный фон
    border: '#37464F',
    borderDark: '#2A3940',

    textPrimary: '#FFFFFF',
    textSecondary: '#8A9BA5',
    textOnPrimary: '#FFFFFF',
    textOnDark: '#FFFFFF',

    success: '#58CC02',
    warning: '#FF9600',
    error: '#FF4B4B',
    info: '#1CB0F6',

    adContainer: '#1B2B32',
    streakFire: '#FF9600',
    factCard: '#2A3940',
    factCardBorder: '#FFE066',
    overlay: 'rgba(0,0,0,0.7)',
  },
};
```

### 1.2 Типографика

Шрифт: **Nunito** (Google Fonts, бесплатный) — округлый, дружелюбный, близок к шрифту Duolingo (DIN Rounded). Жирные начертания для заголовков, создаёт ощущение игривости.

```typescript
// theme/typography.ts
export const typography = {
  fontFamily: {
    regular: 'Nunito_400Regular',
    medium: 'Nunito_500Medium',
    semiBold: 'Nunito_600SemiBold',
    bold: 'Nunito_700Bold',
    extraBold: 'Nunito_800ExtraBold',  // Для заголовков (как Duolingo)
    black: 'Nunito_900Black',          // Для крупных цифр и акцентов
  },
  sizes: {
    // Крупные — заголовки экранов
    displayLarge:  { fontSize: 34, lineHeight: 42, fontFamily: 'Nunito_800ExtraBold' },
    displayMedium: { fontSize: 28, lineHeight: 36, fontFamily: 'Nunito_800ExtraBold' },

    // Заголовки секций
    headlineLarge:  { fontSize: 24, lineHeight: 32, fontFamily: 'Nunito_700Bold' },
    headlineMedium: { fontSize: 20, lineHeight: 28, fontFamily: 'Nunito_700Bold' },

    // Заголовки карточек, кнопок
    titleLarge:  { fontSize: 18, lineHeight: 24, fontFamily: 'Nunito_700Bold' },
    titleMedium: { fontSize: 16, lineHeight: 22, fontFamily: 'Nunito_700Bold' },

    // Основной текст
    bodyLarge:  { fontSize: 17, lineHeight: 24, fontFamily: 'Nunito_600SemiBold' },
    bodyMedium: { fontSize: 15, lineHeight: 22, fontFamily: 'Nunito_400Regular' },
    bodySmall:  { fontSize: 13, lineHeight: 18, fontFamily: 'Nunito_400Regular' },

    // Подписи, лейблы
    labelLarge:  { fontSize: 15, lineHeight: 20, fontFamily: 'Nunito_700Bold' },
    labelMedium: { fontSize: 13, lineHeight: 18, fontFamily: 'Nunito_600SemiBold' },
    labelSmall:  { fontSize: 11, lineHeight: 16, fontFamily: 'Nunito_600SemiBold' },

    // Крупные цифры (стрики, очки, таймеры)
    scoreXL: { fontSize: 48, lineHeight: 56, fontFamily: 'Nunito_900Black' },
    scoreLG: { fontSize: 36, lineHeight: 44, fontFamily: 'Nunito_800ExtraBold' },
  },
};
```

### 1.3 Spacing & Layout

```typescript
// theme/spacing.ts

export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  xxxxl: 48,
  screenPadding: 16,       // Отступ экрана от краёв
  sectionGap: 24,          // Расстояние между секциями
};

// Duolingo-стиль: очень скруглённые углы
export const borderRadius = {
  sm: 8,
  md: 12,
  lg: 16,        // Кнопки, карточки
  xl: 20,        // Модальные окна
  xxl: 24,       // Крупные карточки
  full: 9999,    // Круглые элементы (аватары, бейджи)
};

// 3D-тень в стиле Duolingo (нижняя граница вместо box-shadow)
// Кнопки и карточки имеют толстую нижнюю рамку darker оттенка
export const duoShadow = {
  button: {
    borderBottomWidth: 4,
    // borderBottomColor задаётся в зависимости от варианта кнопки
  },
  card: {
    borderWidth: 2,
    borderBottomWidth: 4,
    // borderColor: colors.border
    // borderBottomColor: colors.borderDark
  },
  cardFlat: {
    borderWidth: 2,
    // borderColor: colors.border
  },
};

// Elevation для тех элементов где нужна настоящая тень (модалки, FAB)
export const elevation = {
  none: { shadowOpacity: 0, elevation: 0 },
  sm: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.08,
    shadowRadius: 4,
    elevation: 2,
  },
  md: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.12,
    shadowRadius: 8,
    elevation: 4,
  },
  lg: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.16,
    shadowRadius: 16,
    elevation: 8,
  },
};
```

### 1.4 Иконки

- **Набор**: @expo/vector-icons — **Feather** для навигации (тонкие, чистые), **MaterialCommunityIcons** для контента (разнообразие)
- **Стиль Tab Bar**: outlined (неактивный) → filled (активный), как в Duolingo
- **Размеры**: 20 (мелкие/бейджи), 24 (стандарт), 28 (таб-бар), 32 (акцент на экранах)
- **Специальные**: иконка огня (streak), звезда (очки), молния (подсказка), книга (факт)

### 1.5 UI-компоненты

---

#### Button (ключевой компонент — 3D стиль Duolingo)
```
Варианты: primary (зелёный), secondary (белый с рамкой), danger (красный),
           blue, orange, ghost (прозрачный)
Размеры: sm (40px), md (50px), lg (56px)
Состояния: default, pressed (translateY +4, тень исчезает), disabled (серый), loading

3D-эффект (как в Duolingo):
  - Нижняя граница 4px более тёмного оттенка
  - При нажатии: translateY(4px), borderBottomWidth: 0 (кнопка «утапливается»)
  - borderRadius: 16px

Структура:
  ├── Animated.View (container, borderRadius: lg)
  │   ├── Icon (optional, left, размер 20)
  │   ├── Text (label, fontFamily: bold, uppercase: false)
  │   └── Icon (optional, right, размер 20)
  └── ActivityIndicator (loading state)

Props: variant, size, label, iconLeft?, iconRight?, loading?, disabled?, onPress, fullWidth?
```

---

#### Card (карточка с 3D-рамкой)
```
Варианты: default (белая с рамкой), highlighted (цветная рамка), flat (без нижней тени)
Структура:
  └── View (borderWidth: 2, borderBottomWidth: 4, borderRadius: xl, padding: lg)
      └── children

Props: variant?, children, onPress?, style?
```

---

#### ProgressBar (прогресс как в Duolingo)
```
Стиль: толстая полоса (10px высота), скруглённые концы, зелёная заливка с анимацией
Варианты: primary (зелёный), blue, orange, gold
Структура:
  └── View (background: surfaceVariant, borderRadius: full, height: 10)
      └── Animated.View (background: primary, borderRadius: full, width: X%)

Props: progress (0-1), variant?, animated?, height?
```

---

#### StreakBadge (бейдж стрика с огнём)
```
Отображение: число + иконка огня (🔥)
Стиль: оранжевый фон, белый текст, fontFamily: black
Анимация: покачивание огня (wiggle) при увеличении стрика
Структура:
  └── View (background: orange, borderRadius: full, paddingH: 12, paddingV: 6)
      ├── FireIcon (анимированный)
      └── Text (число, black, белый)

Props: days: number, animated?: boolean
```

---

#### LetterTile (плитка буквы для игр)
```
Стиль: квадрат 44×44, белый фон, 3D-рамка (как кнопка Duolingo), скругление md
Состояния: default (рамка серая), selected (рамка blue), correct (рамка green),
           incorrect (рамка red), hint (рамка purple)

3D-эффект: borderBottomWidth: 3
При нажатии: утапливается (translateY +3, borderBottom: 0)

Структура:
  └── Pressable (Animated.View)
      └── Text (буква, titleLarge, textPrimary)

Props: letter, state, onPress?, disabled?
```

---

#### WordGrid (сетка для поиска слов)
```
Стиль: сетка букв, каждая ячейка — LetterTile без 3D (плоский стиль), gap: 2px
Свайп: рисование линии по буквам (Gesture Handler)
Линия: полупрозрачная зелёная полоса при правильном слове, красная при неправильном

Props: grid: string[][], foundWords: string[], onWordFound: (word) => void
```

---

#### FactCard (карточка факта — ключевой компонент)
```
Стиль: тёплый бежевый фон (#FFF9E6), золотая рамка, borderRadius: xl
Иконка книги/лампочки вверху
Структура:
  └── View (background: factCard, border: factCardBorder, borderRadius: xl, padding: xxl)
      ├── View (row)
      │   ├── BookIcon (gold, 28)
      │   └── Text ("Интересный факт!", headlineMedium, gold)
      ├── Image (иллюстрация, если есть, borderRadius: lg, marginV: md)
      ├── Text (текст факта, bodyLarge, textPrimary)
      ├── View (row, marginTop: sm)
      │   ├── LinkIcon (12, textSecondary)
      │   └── Text (источник, labelSmall, textSecondary, underline если URL)
      └── View (row, marginTop: xl)
          ├── Button (variant: ghost, label: "Поделиться", iconLeft: share)
          └── Button (variant: primary, label: "Далее →", fullWidth: false)

Props: fact, factSource, factSourceUrl?, illustrationUrl?, onNext, onShare
```

---

#### GameHeader (заголовок игрового экрана)
```
Стиль: без фона, прогресс-бар сверху (как в Duolingo lesson)
Структура:
  └── View (paddingH: screenPadding)
      ├── View (row, spaceBetween)
      │   ├── Pressable (X — закрыть, иконка close)
      │   ├── ProgressBar (progress: currentGame/5, variant: primary)
      │   └── StreakBadge (days: streak) или HeartIcon
      └── Text? (тема дня, labelMedium, textSecondary)

Props: progress, streak, theme?, onClose
```

---

#### Input (поле ввода)
```
Варианты: default, search, answer (для ввода ответа в игре — крупный)
Стиль: borderWidth: 2, borderBottomWidth: 4 (3D), borderRadius: lg
Фокус: borderColor → blue (как Duolingo)
Структура:
  └── View (border: 2/4, borderRadius: lg)
      ├── Icon? (left, 20, textSecondary)
      ├── TextInput (bodyLarge)
      └── Icon? (right — clear button)

Props: variant, value, onChangeText, placeholder, iconLeft?, autoFocus?
```

---

#### BottomTabBar
```
Стиль: белый фон, верхняя граница (borderTopWidth: 2, border), без тени
Табы: 4 штуки — Home, Infinite, Leaderboard, Profile
Иконки: 28px, outlined → filled при активном
Активный: primary (зелёный), неактивный: textSecondary (серый)
Лейбл: labelSmall под иконкой
Высота: 56px + safe area bottom

Табы:
  🏠 Главная  |  ♾️ Режим  |  🏆 Рейтинг  |  👤 Профиль
```

---

#### Toast / Snackbar
```
Варианты: success (зелёный), error (красный), info (синий), warning (оранжевый)
Стиль: borderRadius: lg, padding: lg, 3D-рамка снизу
Позиция: сверху (под status bar), slide down + slide up
Длительность: 3 сек
Haptic: success → notificationSuccess, error → notificationError
```

---

#### Skeleton
```
Стиль: surfaceVariant с shimmer-анимацией (градиент бегущий слева направо)
Формы: rectangle (для текста), circle (для аватаров), card (для карточек)
borderRadius: как у заменяемого элемента
```

---

#### EmptyState
```
Структура:
  └── View (center, padding: xxxxl)
      ├── Illustration (Lottie или SVG, 200×200)
      ├── Text (заголовок, headlineMedium, center)
      ├── Text (описание, bodyMedium, textSecondary, center)
      └── Button (CTA, variant: primary, marginTop: xl)
```

---

#### ErrorState
```
Структура:
  └── View (center, padding: xxxxl)
      ├── Illustration (грустный персонаж, 150×150)
      ├── Text ("Упс! Что-то пошло не так", headlineMedium, center)
      ├── Text (сообщение ошибки, bodyMedium, textSecondary, center)
      └── Button ("Попробовать снова", variant: primary)
```

---

#### AdBanner (контейнер для рекламы)
```
Стиль: surfaceVariant фон, borderRadius: md, padding: xs
Минимальная стилизация, чтобы реклама не выглядела чужеродно
Высота: 50px (320×50 banner) + padding 8px сверху/снизу = 66px total
При ошибке загрузки: height → 0 (анимированное схлопывание)
Подпись мелким шрифтом: "Реклама" (labelSmall, textSecondary)
```

---

#### DailyResultCard (экран результатов ежедневного набора)
```
Стиль: крупная цифра по центру (scoreXL), цвет зависит от результата:
  5/5 → gold + конфетти, 4/5 → primary, 3/5 → blue, ≤2/5 → orange
Структура:
  └── View (center)
      ├── AnimatedScore (большая цифра "4/5", scoreXL)
      ├── Text ("Отличная работа!", headlineLarge)
      ├── StreakBadge
      ├── LeaderboardPosition (#42 из 500)
      ├── Button ("Поделиться", variant: blue, iconLeft: share)
      └── Button ("На главный", variant: secondary)
```

---

#### Switch / Toggle
```
Стиль: как iOS switch но в цветах бренда
Вкл: primary (зелёный track), белый thumb
Выкл: surfaceVariant (серый track), белый thumb
Размер: 51×31 (стандарт iOS)
```

---

#### Badge
```
Стиль: круг, фон primary/red/orange/blue, белый текст, fontSize: labelSmall
Размер: 18×18 (min), растягивается по контенту
Позиция: absolute top-right от родительского элемента
```

---

#### Chip / Tag
```
Стиль: borderRadius: full, paddingH: 12, paddingV: 6, border: 2px
Варианты: outline (текст + рамка), filled (фон + текст)
Цвета: primary, blue, orange, purple, default (серый)
```

---

#### Avatar
```
Стиль: круг, borderRadius: full, border: 3px primary
Размеры: sm (32), md (44), lg (64), xl (88)
Fallback: первая буква nickname, фон primary, белый текст
```

---

#### Divider
```
Стиль: View height: 2, background: border, marginV: lg
```

---

#### ListItem
```
Стиль: белый фон, borderRadius: lg, paddingV: md, paddingH: lg
Варианты: default (без рамки), card (с 3D-рамкой)
Структура:
  └── Pressable
      ├── Icon/Avatar (left)
      ├── View (flex: 1)
      │   ├── Text (title, titleMedium)
      │   └── Text (subtitle, bodySmall, textSecondary)
      └── ChevronRight / Value (right)
```

---

#### Modal / BottomSheet
```
Стиль: background: surface, borderTopLeftRadius: xxl, borderTopRightRadius: xxl
Handle: серая полоска 40×4, borderRadius: full, center, marginTop: sm
Overlay: rgba(0,0,0,0.5) с fade
Анимация: slide up (spring, damping: 20)
```

---

## 2. Карта экранов

### Навигация (expo-router)
```
App
├── (onboarding)/
│   ├── _layout.tsx              # Stack, без header
│   ├── step-1.tsx               # Ценность приложения
│   ├── step-2.tsx               # Как это работает
│   └── step-3.tsx               # Выбор языка контента
├── (tabs)/
│   ├── _layout.tsx              # BottomTabBar (4 таба)
│   ├── home/
│   │   ├── _layout.tsx          # Stack
│   │   └── index.tsx            # Home Screen (главный)
│   ├── infinite/
│   │   ├── _layout.tsx          # Stack
│   │   └── index.tsx            # Бесконечный режим (вход)
│   ├── leaderboard/
│   │   ├── _layout.tsx          # Stack
│   │   └── index.tsx            # Лидерборд
│   └── profile/
│       ├── _layout.tsx          # Stack
│       ├── index.tsx            # Профиль + статистика
│       └── settings.tsx         # Настройки
├── game/
│   ├── _layout.tsx              # Stack, fullscreen, без tab bar
│   ├── daily.tsx                # Обёртка ежедневного набора
│   ├── anagram.tsx              # Мини-игра: Анаграмма
│   ├── compose.tsx              # Мини-игра: Составь слова
│   ├── chain.tsx                # Мини-игра: Цепочка слов
│   ├── search.tsx               # Мини-игра: Поиск слов
│   └── guess.tsx                # Мини-игра: Угадай слово (Wordle)
├── modal/
│   ├── fact.tsx                 # Карточка факта (modal presentation)
│   ├── results.tsx              # Результаты ежедневного набора
│   ├── nickname.tsx             # Ввод никнейма
│   └── streak-milestone.tsx     # Празднование milestone стрика
└── _layout.tsx                  # Root layout
```

### Список экранов

| # | Экран | Назначение | Навигация | Рекламный блок | Состояния |
|---|-------|-----------|-----------|----------------|-----------|
| 1 | Splash | Лого + загрузка | — | Нет | loading |
| 2 | Onboarding 1 | Ценность | Stack | Нет | filled |
| 3 | Onboarding 2 | Как работает | Stack | Нет | filled |
| 4 | Onboarding 3 | Выбор языка | Stack | Нет | filled |
| 5 | Home | Ежедневный набор + стрик | Tab 1 | Banner внизу | loading, filled, offline |
| 6 | Infinite Entry | Вход в бесконечный режим | Tab 2 | Banner внизу | loading, filled |
| 7 | Leaderboard | Рейтинг игроков | Tab 3 | Banner внизу | loading, filled, empty |
| 8 | Profile | Статистика + настройки | Tab 4 | Нет | loading, filled |
| 9 | Settings | Язык, тема, push, звуки | Stack (Profile) | Нет | filled |
| 10 | Game: Anagram | Мини-игра анаграмма | Fullscreen Stack | Нет | filled |
| 11 | Game: Compose | Мини-игра составь слова | Fullscreen Stack | Нет | filled |
| 12 | Game: Chain | Мини-игра цепочка | Fullscreen Stack | Нет | filled |
| 13 | Game: Search | Мини-игра поиск слов | Fullscreen Stack | Нет | filled |
| 14 | Game: Guess | Мини-игра Wordle | Fullscreen Stack | Нет | filled |
| 15 | Fact Card | Факт + иллюстрация + источник | Modal | Нет | filled |
| 16 | Daily Results | Результаты набора | Modal | Interstitial (перед) | filled |
| 17 | Nickname | Ввод никнейма | Modal | Нет | filled |
| 18 | Streak Milestone | Празднование 7/30/100 дней | Modal | Нет | filled |

---

## 3. Детальное описание экранов

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Home Screen
### Файл: app/(tabs)/home/index.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT:
┌─────────────────────────────────┐
│ ░░░░░░ StatusBar ░░░░░░░░░░░░░░│
├─────────────────────────────────┤
│                                 │
│  WordPulse          🔥 14       │  ← Логотип + StreakBadge
│                                 │
├─────────────────────────────────┤
│                                 │
│  ╔═══════════════════════════╗  │
│  ║  📅 Ежедневный набор      ║  │  ← Card с 3D-рамкой
│  ║                           ║  │
│  ║  Тема: Космическая        ║  │
│  ║        лексика 🚀         ║  │
│  ║                           ║  │
│  ║  ████████░░░░░░░  3/5     ║  │  ← ProgressBar
│  ║                           ║  │
│  ║  ┌─────────────────────┐  ║  │
│  ║  │   ▶ ПРОДОЛЖИТЬ      │  ║  │  ← Button primary, 3D, full width
│  ║  └─────────────────────┘  ║  │
│  ║                           ║  │
│  ╚═══════════════════════════╝  │
│                                 │
│  ╔═══════════════════════════╗  │
│  ║  ♾️  Бесконечный режим    ║  │  ← Card (blue accent)
│  ║                           ║  │
│  ║  Случайные головоломки    ║  │
│  ║  без ограничений          ║  │
│  ║                           ║  │
│  ║  ┌─────────────────────┐  ║  │
│  ║  │      ИГРАТЬ         │  ║  │  ← Button blue, 3D
│  ║  └─────────────────────┘  ║  │
│  ╚═══════════════════════════╝  │
│                                 │
│  ┌─────────────────────────────┐│
│  │     Рекламный баннер        ││  ← AdBanner 320×50
│  └─────────────────────────────┘│
│                                 │
├─────────────────────────────────┤
│  🏠      ♾️      🏆      👤    │  ← BottomTabBar
│ Главная Режим  Рейтинг Профиль │
└─────────────────────────────────┘
```

**ЭЛЕМЕНТЫ:**
1. Header: высота 56px, без рамки, логотип слева (headlineLarge, primary), StreakBadge справа
2. Daily Set Card: borderWidth: 2, borderBottomWidth: 4, borderRadius: xl, padding: xxl
3. ProgressBar: height: 10, primary, показывает X/5
4. CTA Button: primary, 3D, lg, full width, borderRadius: lg
5. Infinite Mode Card: border blue, CTA button blue
6. AdBanner: 66px total height, внизу перед tab bar

**ПОВЕДЕНИЕ:**
- При свайпе вниз: Pull-to-refresh (обновить набор с сервера)
- Нажатие «Продолжить» → переход в game/daily (fullscreen, tab bar скрыт)
- Нажатие «Играть» (бесконечный) → переход в game с random вопросом
- Если набор завершён: карточка показывает результат + обратный отсчёт «Новый набор через 5ч 23м»

**АНИМАЦИИ:**
- StreakBadge: покачивание огня при загрузке (wiggle, 0.5s)
- ProgressBar: плавное заполнение при загрузке (spring, 0.8s)
- Карточки: fade in + slide up при появлении (stagger 100ms)
- Pull-to-refresh: bounce

**СОСТОЯНИЯ:**
- Loading: Skeleton (2 карточки + progress bar shape)
- Filled: как на макете выше
- Offline: жёлтый Banner сверху «Нет подключения. Используем кэш» + кэшированный набор
- Set completed: Daily Card меняет вид → зелёный фон, «✅ Набор пройден! 4/5», обратный отсчёт

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Game — Анаграмма (пример игрового экрана)
### Файл: app/game/anagram.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT:
┌─────────────────────────────────┐
│ ░░░░░░ StatusBar ░░░░░░░░░░░░░░│
├─────────────────────────────────┤
│                                 │
│  ✕    ████████░░░  3/5   🔥14  │  ← GameHeader (close + progress + streak)
│                                 │
├─────────────────────────────────┤
│                                 │
│         Составь слово           │  ← headlineLarge, center
│    из перемешанных букв         │  ← bodyMedium, textSecondary
│                                 │
│                                 │
│     ┌───┐ ┌───┐ ┌───┐ ┌───┐   │
│     │   │ │   │ │   │ │   │   │  ← Зона ответа (пустые слоты)
│     └───┘ └───┘ └───┘ └───┘   │
│                                 │
│                                 │
│                                 │
│     ┌───┐ ┌───┐ ┌───┐ ┌───┐   │
│     │ О │ │ С │ │ Л │ │ В │   │  ← LetterTiles (перемешанные)
│     └───┘ └───┘ └───┘ └───┘   │
│                                 │
│                                 │
│  ┌─────┐              ┌──────┐ │
│  │ 💡  │              │ГОТОВО│ │  ← Подсказка (rewarded) + Проверить
│  └─────┘              └──────┘ │
│                                 │
└─────────────────────────────────┘
```

**ЭЛЕМЕНТЫ:**
1. GameHeader: закрыть (✕), ProgressBar (3/5), StreakBadge — высота 48px
2. Задание: headlineLarge + bodyMedium, center, marginTop: xxxl
3. Зона ответа: ряд пустых LetterTile (border dashed, borderColor: border), 44×44
4. Буквы: ряд LetterTile (3D, active), 48×48, gap: 8
5. Кнопка «Подсказка»: ghost, sm, iconLeft: bulb (purple)
6. Кнопка «Готово»: primary, md, 3D

**ПОВЕДЕНИЕ:**
- Тап на букву внизу → буква перелетает вверх в первый пустой слот (анимация spring)
- Тап на букву вверху → буква возвращается вниз
- Кнопка «Готово» → проверка ответа на сервере
- Правильно: все плитки → зелёные (одна за другой, stagger 100ms), haptic success, через 500ms → открытие Fact Card
- Неправильно: все плитки → красные (shake анимация 300ms), haptic error, через 1s → показать правильный ответ → Fact Card

**АНИМАЦИИ:**
- Буквы при загрузке: bounce in (stagger)
- Перемещение буквы: spring (damping: 15) из позиции A в позицию B
- Правильный ответ: scale(1.1) + зелёный цвет (stagger), confetti сверху
- Неправильный ответ: shakeX (3 цикла, 50ms)
- Haptic: impactLight при тапе на букву, notificationSuccess при правильном, notificationError при неправильном

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Fact Card
### Файл: app/modal/fact.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT (Modal, slide up from bottom):
┌─────────────────────────────────┐
│                                 │
│         (оверлей 50%)           │
│                                 │
├─────────────────────────────────┤
│  ┌─────────────────────────────┐│
│  │         ─── (handle)        ││
│  │                             ││
│  │  📖 Интересный факт!       ││  ← BookIcon + headlineMedium, gold
│  │                             ││
│  │  ┌───────────────────────┐  ││
│  │  │                       │  ││
│  │  │    🖼 Иллюстрация     │  ││  ← Image, borderRadius: lg, 200px
│  │  │                       │  ││
│  │  └───────────────────────┘  ││
│  │                             ││
│  │  Слово «космос» пришло в   ││  ← bodyLarge, textPrimary
│  │  русский язык из           ││
│  │  древнегреческого κόσμος,  ││
│  │  что изначально означало   ││
│  │  «порядок, устройство»...  ││
│  │                             ││
│  │  🔗 Wikipedia: Космос      ││  ← labelSmall, textSecondary, underline
│  │                             ││
│  │  ┌──────────┐ ┌──────────┐ ││
│  │  │Поделиться│ │ Далее →  │ ││  ← ghost + primary buttons
│  │  └──────────┘ └──────────┘ ││
│  └─────────────────────────────┘│
└─────────────────────────────────┘
```

**ЭЛЕМЕНТЫ:**
1. Handle: серая полоска 40×4, center
2. Заголовок: BookIcon (gold, 24) + «Интересный факт!» (headlineMedium, color: gold)
3. Иллюстрация: Image, borderRadius: lg, width: 100%, aspectRatio: 16/9, опциональна
4. Текст факта: bodyLarge, textPrimary, до 500 символов
5. Источник: LinkIcon (12) + текст (labelSmall, textSecondary), при наличии URL — underline, onPress → Linking.openURL
6. Кнопки: «Поделиться» (ghost, blue) + «Далее →» (primary, 3D)

**АНИМАЦИИ:**
- Modal slide up: spring (damping: 20, stiffness: 300)
- Иллюстрация: fade in (300ms delay)
- Текст факта: fade in + slide up (stagger по абзацам)
- Кнопки: fade in (500ms delay)

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Daily Results
### Файл: app/modal/results.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT (Fullscreen modal):
┌─────────────────────────────────┐
│                                 │
│     ✨ (конфетти если 5/5) ✨    │
│                                 │
│             ⭐                   │
│            4/5                  │  ← scoreXL, gold/primary
│       Отличная работа!          │  ← headlineLarge
│                                 │
│     🔥 Стрик: 14 дней          │  ← StreakBadge (large)
│                                 │
│  ╔═══════════════════════════╗  │
│  ║  Твоя позиция: #42       ║  │  ← Card
│  ║  из 487 игроков сегодня   ║  │
│  ╚═══════════════════════════╝  │
│                                 │
│  🟩🟩🟩🟩🟥                    │  ← 5 квадратиков (правильно/нет)
│                                 │
│  ┌─────────────────────────────┐│
│  │      📤 ПОДЕЛИТЬСЯ          ││  ← Button blue, full width
│  └─────────────────────────────┘│
│  ┌─────────────────────────────┐│
│  │      🏠 НА ГЛАВНУЮ          ││  ← Button secondary, full width
│  └─────────────────────────────┘│
│                                 │
└─────────────────────────────────┘
```

**АНИМАЦИИ:**
- Число (4/5): count up от 0 (500ms), bounce at finish
- Звезда: scale from 0 → 1.2 → 1.0 (spring)
- Конфетти: при 5/5 — полноэкранная Lottie-анимация конфетти (2s)
- StreakBadge: slide in from left (300ms delay)
- Позиция: fade in (500ms delay)
- Кнопки: fade in (700ms delay)

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Game — Угадай слово (Wordle)
### Файл: app/game/guess.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT:
┌─────────────────────────────────┐
│ ░░░░░░ StatusBar ░░░░░░░░░░░░░░│
├─────────────────────────────────┤
│  ✕    ██████████░░  5/5  🔥14  │  ← GameHeader
├─────────────────────────────────┤
│                                 │
│  Угадай слово за 6 попыток     │  ← headlineMedium, center
│                                 │
│  ┌──┐┌──┐┌──┐┌──┐┌──┐         │  ← Ряд 1 (заполнен)
│  │🟩││🟨││⬜││⬜││🟩│         │
│  └──┘└──┘└──┘└──┘└──┘         │
│  ┌──┐┌──┐┌──┐┌──┐┌──┐         │  ← Ряд 2 (текущий ввод)
│  │ К││ О││ С││  ││  │         │
│  └──┘└──┘└──┘└──┘└──┘         │
│  ┌──┐┌──┐┌──┐┌──┐┌──┐         │  ← Ряд 3-6 (пустые)
│  │  ││  ││  ││  ││  │         │
│  └──┘└──┘└──┘└──┘└──┘         │
│  ... (ещё 3 ряда)              │
│                                 │
│  ┌─────────────────────────┐   │
│  │ Й Ц У К Е Н Г Ш Щ З Х │   │  ← Клавиатура
│  │  Ф Ы В А П Р О Л Д Ж  │   │    (цветные подсказки на клавишах)
│  │ 💡 Я Ч С М И Т Ь Б Ю ⌫│   │
│  └─────────────────────────┘   │
│                                 │
│  ┌──────────────────────────┐  │
│  │        ПРОВЕРИТЬ          │  │  ← Button primary, full width
│  └──────────────────────────┘  │
└─────────────────────────────────┘
```

**ЭЛЕМЕНТЫ:**
- Сетка 6×5: LetterTiles, 48×48, gap: 4
- Цвета: 🟩 primary (#58CC02) = верная позиция, 🟨 gold (#FFC800) = есть в слове, ⬜ surfaceVariant = нет в слове
- Клавиатура: кастомная (не системная), клавиши с 3D-эффектом, цвет зависит от использования
- 💡 = подсказка (rewarded video), ⌫ = удалить последнюю букву

**АНИМАЦИИ:**
- Ввод буквы: scale(1.1) → 1.0 (pop in, 100ms)
- Проверка ряда: flip каждой плитки (rotate3D X, 300ms stagger), цвет появляется после flip
- Неправильное слово (нет в словаре): shakeX всего ряда
- Победа: bounce каждой зелёной плитки (stagger), 🎉 → Fact Card

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Leaderboard
### Файл: app/(tabs)/leaderboard/index.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT:
┌─────────────────────────────────┐
│ ░░░░░░ StatusBar ░░░░░░░░░░░░░░│
├─────────────────────────────────┤
│  🏆 Рейтинг                    │  ← headlineLarge
│                                 │
│  ┌─────────┬─────────┬────────┐│
│  │ Сегодня │ Неделя  │  Всё   ││  ← Tabs (Chip-стиль, selected = filled)
│  └─────────┴─────────┴────────┘│
│                                 │
│  ╔═══════════════════════════╗  │
│  ║ 🥇 #1  wordking    950   ║  │  ← Gold card, 3D
│  ╚═══════════════════════════╝  │
│  ╔═══════════════════════════╗  │
│  ║ 🥈 #2  brainiac    920   ║  │  ← Silver card
│  ╚═══════════════════════════╝  │
│  ╔═══════════════════════════╗  │
│  ║ 🥉 #3  puzzlefan   905   ║  │  ← Bronze card
│  ╚═══════════════════════════╝  │
│                                 │
│  ┌─────────────────────────────┐│
│  │ #4  player123       890    ││  ← ListItem (обычный)
│  ├─────────────────────────────┤│
│  │ #5  словолюб        875    ││
│  ├─────────────────────────────┤│
│  │ ...                        ││
│  └─────────────────────────────┘│
│                                 │
│  ╔═══════════════════════════╗  │
│  ║ Ты: #42  myname    650   ║  │  ← Sticky footer, highlighted (blue border)
│  ╚═══════════════════════════╝  │
│                                 │
│  ┌─────────────────────────────┐│
│  │     Рекламный баннер        ││
│  └─────────────────────────────┘│
├─────────────────────────────────┤
│  🏠      ♾️      🏆      👤    │
└─────────────────────────────────┘
```

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Profile (Статистика)
### Файл: app/(tabs)/profile/index.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT:
┌─────────────────────────────────┐
│ ░░░░░░ StatusBar ░░░░░░░░░░░░░░│
├─────────────────────────────────┤
│  👤 Профиль               ⚙️   │  ← headlineLarge + Settings icon
│                                 │
│           ┌────┐                │
│           │ АН │                │  ← Avatar (xl, первые буквы)
│           └────┘                │
│         player123               │  ← nickname, titleLarge
│                                 │
│  ┌────────┐ ┌────────┐ ┌─────┐ │
│  │ 🔥 14  │ │ ⭐ 350 │ │📖 89│ │  ← Stat cards (streak, score, facts)
│  │ стрик  │ │ очков  │ │фактов│ │
│  └────────┘ └────────┘ └─────┘ │
│                                 │
│  Активность                     │  ← headlineMedium
│  ┌─────────────────────────────┐│
│  │  Пн Вт Ср Чт Пт Сб Вс    ││  ← Тепловая карта (GitHub-стиль)
│  │  🟩 🟩 🟩 ⬜ 🟩 🟩 ⬜     ││     Зелёные = играл, серые = пропуск
│  │  🟩 🟩 ⬜ 🟩 🟩 🟩 🟩     ││
│  │  ...                       ││
│  └─────────────────────────────┘│
│                                 │
│  Статистика                     │
│  ┌─────────────────────────────┐│
│  │ Всего игр          234     ││  ← ListItems
│  │ Правильных          78%    ││
│  │ Лучший стрик        21     ││
│  │ Средний балл        3.8/5  ││
│  └─────────────────────────────┘│
│                                 │
├─────────────────────────────────┤
│  🏠      ♾️      🏆      👤    │
└─────────────────────────────────┘
```

---

### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
### ЭКРАН: Onboarding (3 шага)
### Файлы: app/(onboarding)/step-1..3.tsx
### ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
LAYOUT (каждый шаг):
┌─────────────────────────────────┐
│                                 │
│          Пропустить →           │  ← labelLarge, textSecondary, top-right
│                                 │
│                                 │
│         ┌─────────────┐         │
│         │             │         │
│         │  Lottie /   │         │  ← Анимированная иллюстрация, 250×250
│         │  SVG        │         │
│         │             │         │
│         └─────────────┘         │
│                                 │
│    Играй в слова —              │  ← displayMedium, center
│    узнавай реальные             │
│    факты!                       │
│                                 │
│    Каждый день новые            │  ← bodyLarge, textSecondary, center
│    головоломки и                │
│    интересные факты             │
│    из проверенных               │
│    источников                   │
│                                 │
│         ● ○ ○                   │  ← Pagination dots (primary / border)
│                                 │
│  ┌─────────────────────────────┐│
│  │        ПРОДОЛЖИТЬ            ││  ← Button primary, 3D, full width, lg
│  └─────────────────────────────┘│
│                                 │
└─────────────────────────────────┘
```

---

## 4. Микро-взаимодействия и анимации

| Действие | Анимация | Длительность | Haptic | Библиотека |
|----------|----------|-------------|--------|------------|
| Тап на кнопку 3D | translateY(+4), borderBottomWidth → 0 | 50ms | impactLight | Reanimated |
| Отпускание кнопки | translateY(0), borderBottomWidth → 4 | 100ms (spring) | — | Reanimated |
| Тап на LetterTile | scale(0.9) → 1.0 | 100ms | impactLight | Reanimated |
| Перемещение буквы (анаграмма) | layout animation (spring, damping: 15) | 300ms | — | Reanimated Layout |
| Правильный ответ — плитки | зелёный цвет + scale(1.1), stagger 100ms | 500ms total | notificationSuccess | Reanimated |
| Неправильный ответ — плитки | красный цвет + shakeX (3 цикла) | 300ms | notificationError | Reanimated |
| Wordle flip (проверка ряда) | rotateX(90°) → цвет → rotateX(0°), stagger | 250ms × 5 | impactMedium per tile | Reanimated |
| Конфетти (5/5 результат) | Lottie анимация confetti | 2s | notificationSuccess | lottie-react-native |
| Streak milestone (7/30/100) | Scale bounce + shine effect + confetti | 2s | notificationSuccess | Reanimated + Lottie |
| Fact Card appearance | Modal slide up (spring) | 400ms | — | React Navigation |
| Fact Card image | Fade in | 300ms delay | — | Reanimated |
| ProgressBar fill | width animation (spring) | 800ms | — | Reanimated |
| Score count up (results) | 0 → N (interpolation) | 500ms | — | Reanimated |
| Tab switch | Cross-fade content, icon fill/outline swap | 200ms | impactLight | React Navigation |
| Pull-to-refresh | Bounce down → spinner → bounce up | native | — | FlatList native |
| Toast appear | slideInDown (spring) → 3s → slideOutUp | 300ms + 300ms | varies | Reanimated |
| Skeleton shimmer | LinearGradient translateX loop | 1.5s loop | — | expo-linear-gradient |
| Card appear (stagger) | fade in + translateY(20 → 0), stagger 100ms | 300ms each | — | Reanimated |

---

## 5. Адаптивность

- **Телефоны (320px - 428px)**: основной таргет, single column layout
- **Планшеты (768px+)**: не приоритет для MVP; контент центрирован с maxWidth: 500px
- **Safe Areas**: SafeAreaView на всех экранах
  - Верх: StatusBar + notch/Dynamic Island
  - Низ: home indicator + tab bar
- **Ориентация**: Portrait only (заблокировать landscape в app.json)
- **Шрифт**: поддержка системного масштабирования (allowFontScaling: true, но с maxFontSizeMultiplier: 1.3 чтобы не ломать layout)
- **LetterTiles**: адаптивный размер в зависимости от ширины экрана (экран / кол-во букв - gap)

---

## 6. App Icon и Splash Screen

### Иконка приложения
- **Форма**: буква **W** в стиле LetterTile (3D-плитка Duolingo-стиля)
- **Фон**: primary (#58CC02) — яркий зелёный
- **Буква**: белая, жирная (Nunito ExtraBold), с лёгкой 3D-тенью внизу
- **Стиль**: чистый, без лишних деталей, узнаваемый в маленьком размере
- **iOS**: без альфа-канала, без скруглений в файле (iOS сам скругляет)
- **Android**: adaptive icon — foreground (W + тень) + background (сплошной primary)
- **Размер исходника**: 1024×1024

### Splash Screen
- **Фон**: primary (#58CC02)
- **Логотип**: белая буква W по центру (как на иконке, но крупнее)
- **expo-splash-screen**: `resizeMode: "contain"`, `backgroundColor: "#58CC02"`
- **Анимация перехода**: fade out splash → fade in Home Screen (300ms)

---

## Резюме для Tech Lead

### Используемые npm-пакеты для UI

| Пакет | Назначение |
|-------|-----------|
| `@expo-google-fonts/nunito` | Шрифт Nunito (все начертания) |
| `react-native-reanimated` | Анимации (3D-кнопки, переходы, shake, flip) |
| `react-native-gesture-handler` | Жесты (свайпы в Word Search, drag букв) |
| `lottie-react-native` | Lottie-анимации (конфетти, milestone, onboarding иллюстрации) |
| `expo-haptics` | Haptic feedback (тап, успех, ошибка) |
| `expo-linear-gradient` | Skeleton shimmer эффект |
| `@gorhom/bottom-sheet` | Bottom Sheet (Fact Card, модалки) |
| `expo-splash-screen` | Splash Screen |
| `@expo/vector-icons` | Иконки (Feather + MaterialCommunityIcons) |
| `react-native-safe-area-context` | Safe Areas |
| `react-native-svg` | SVG-иллюстрации |
| `expo-image` | Оптимизированная загрузка иллюстраций (кэширование) |

### Все экраны с файловой структурой expo-router

```
app/
├── _layout.tsx
├── (onboarding)/
│   ├── _layout.tsx
│   ├── step-1.tsx
│   ├── step-2.tsx
│   └── step-3.tsx
├── (tabs)/
│   ├── _layout.tsx          # BottomTabBar (4 таба)
│   ├── home/
│   │   ├── _layout.tsx
│   │   └── index.tsx        # Home Screen
│   ├── infinite/
│   │   ├── _layout.tsx
│   │   └── index.tsx        # Бесконечный режим
│   ├── leaderboard/
│   │   ├── _layout.tsx
│   │   └── index.tsx        # Лидерборд
│   └── profile/
│       ├── _layout.tsx
│       ├── index.tsx         # Профиль + статистика
│       └── settings.tsx      # Настройки
├── game/
│   ├── _layout.tsx           # Fullscreen, без tab bar
│   ├── daily.tsx             # Обёртка ежедневного набора
│   ├── anagram.tsx
│   ├── compose.tsx
│   ├── chain.tsx
│   ├── search.tsx
│   └── guess.tsx
└── modal/
    ├── fact.tsx              # Карточка факта
    ├── results.tsx           # Результаты набора
    ├── nickname.tsx          # Ввод никнейма
    └── streak-milestone.tsx  # Празднование стрика
```

### Ключевые технические требования к UI

1. **3D-кнопки Duolingo-стиля**: borderBottomWidth: 4 + translateY при нажатии (Reanimated)
2. **LetterTile как базовый компонент**: используется в 4 из 5 мини-игр, должен быть высокопроизводительным
3. **Кастомная клавиатура** для Wordle: не системная, с цветной подсветкой использованных букв
4. **Gesture Handler** для Word Search: рисование линии по сетке букв
5. **Layout animations** для анаграмм: перемещение букв между позициями
6. **Lottie**: конфетти, onboarding-иллюстрации, streak-celebrations (3-5 файлов)
7. **Тёмная тема**: полная поддержка через ThemeProvider + useColorScheme
8. **Шрифт Nunito**: 6 начертаний, загрузка через expo-font при старте
9. **expo-image**: для иллюстраций к фактам (кэширование, placeholder, fade-in)
10. **Адаптивные LetterTiles**: размер рассчитывается от ширины экрана
