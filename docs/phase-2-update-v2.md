# Фаза 2: Бизнес-аналитик — Обновление требований v2

> **Дата:** 2026-02-23
> **Входные данные:** Текущая реализация (server, mobile, web) + новые требования от владельца
> **Статус:** Обновление существующих требований из `phase-2-requirements.md`

---

## Обзор изменений

### Три ключевые задачи:
1. **Подборки вопросов на главном экране** — система коллекций с фильтрацией
2. **Редизайн UI** — Apple Human Interface Guidelines как основа
3. **Исправление API** — баги в activity, ranking, streak

---

## 1. ПОДБОРКИ ВОПРОСОВ НА ГЛАВНОМ ЭКРАНЕ

### 1.1 Концепция

**Текущее состояние:** Главный экран содержит единственную карточку «Daily Set» (ежедневный набор 15 утверждений).

**Новое состояние:** Главный экран — это витрина подборок (коллекций) с горизонтальной и вертикальной навигацией, фильтрацией по темам и сложности. Пользователь видит разнообразный контент и может выбрать, во что играть прямо сейчас.

### 1.2 Типы подборок (коллекций)

#### Тип 1: Daily (Ежедневная подборка)

| Параметр | Значение |
|----------|----------|
| Количество вопросов | 15 |
| Доступность | Одна новая каждый день |
| После прохождения | **Заблокирована до следующей недели** (не до следующего дня!) |
| Отображение | Всегда на первом месте на главном экране |
| Визуальный стиль | Выделенная карточка с таймером «Новая через X дн.» |
| Стрик | Засчитывается в серию дней |

**Логика блокировки:**
- Пользователь прошёл Daily → карточка показывает результат + обратный отсчёт до следующего понедельника (или 7 дней с момента прохождения)
- Новая Daily-подборка доступна раз в неделю (по понедельникам) или через 7 дней после прохождения предыдущей
- Если пользователь пропустил неделю — доступна текущая Daily, предыдущие не накапливаются

#### Тип 2: Тематические подборки (по категориям)

| Параметр | Значение |
|----------|----------|
| Категории | Наука, Спорт, История, География, Природа, Космос, Культура, Технологии, Здоровье, Еда, Языки, Логика |
| Количество вопросов | 10-15 (настраивается в админке) |
| Доступность | Всегда доступны, неограниченное количество |
| После прохождения | Можно пройти заново (вопросы будут другие — антиповтор) |
| Отображение | Горизонтальный скролл с иконками категорий |

#### Тип 3: По сложности

| Параметр | Значение |
|----------|----------|
| Уровни | Лёгкие (1-2), Средние (3), Сложные (4-5) |
| Количество вопросов | 10 |
| Доступность | Всегда доступны |
| После прохождения | Генерируется новый набор из пула |
| Отображение | Секция «По сложности» с тремя карточками |

#### Тип 4: Специальные/Тематические подборки (создаются в админке)

| Параметр | Значение |
|----------|----------|
| Примеры | «Мифы о здоровье», «Космические рекорды», «Странные законы», «Олимпийские факты» |
| Количество вопросов | 10-20 (настраивается) |
| Доступность | По расписанию или постоянно |
| После прохождения | Показываются результаты, можно пройти заново |
| Отображение | Секция «Рекомендуемые» или «Новые подборки» |

### 1.3 Функциональные требования — Подборки

---

**FR-100: Главный экран с подборками**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как пользователь, я хочу видеть различные подборки вопросов на главном экране, чтобы выбрать интересную тему и не ограничиваться одним ежедневным набором.
- Описание: Главный экран отображает секции с подборками в вертикальном скролле:
  1. **Секция «Daily»** — ежедневная подборка (выделена визуально, одна штука)
  2. **Секция «Темы»** — горизонтальный скролл категорий (иконка + название + количество доступных вопросов)
  3. **Секция «По сложности»** — три карточки: Лёгкие / Средние / Сложные
  4. **Секция «Подборки»** — специальные тематические подборки от админа (горизонтальный скролл карточек)
- Критерии приёмки:
  - AC-1: Когда пользователь открывает Home, тогда отображаются все 4 секции
  - AC-2: Когда пользователь скроллит горизонтально в секции «Темы», тогда видны все доступные категории
  - AC-3: Когда пользователь тапает на категорию, тогда начинается набор из 10 случайных вопросов этой категории
  - AC-4: Когда пользователь тапает на карточку сложности, тогда начинается набор из 10 вопросов указанной сложности
  - AC-5: Когда пользователь тапает на специальную подборку, тогда открывается её страница с описанием и кнопкой «Начать»
- Связанные экраны: Home Screen (обновлённый)
- Зависимости: FR-101, FR-102, FR-103
- Требует backend: Да

---

**FR-101: Daily подборка с недельной блокировкой**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как пользователь, я хочу проходить ежедневную подборку и ждать следующей недели, чтобы ощущать эксклюзивность и ценность каждого набора.
- Описание: Daily подборка содержит 15 вопросов. После завершения подборка блокируется на 7 дней. Показывается обратный отсчёт «Новая подборка через X дн. Y ч.». Стрик считается по факту прохождения Daily-подборок (хотя бы одна в неделю поддерживает стрик, пропуск недели — сброс).
- Критерии приёмки:
  - AC-1: Когда пользователь завершил Daily, тогда карточка показывает результат и таймер до следующей
  - AC-2: Когда прошло 7 дней после завершения, тогда новая Daily становится доступна
  - AC-3: Когда пользователь не завершил Daily за неделю, тогда предыдущая заменяется новой
  - AC-4: Когда Daily заблокирована, тогда кнопка «Начать» недоступна, показан таймер
  - AC-5: Когда пользователь пропустил неделю, тогда стрик сбрасывается
- Связанные экраны: Home Screen → Daily Card
- Требует backend: Да (новая логика блокировки)
- API изменения:
  - `GET /api/v1/daily-sets/current` — возвращает текущий доступный daily set + статус блокировки + время до следующего
  - Ответ: `{ set: DailySet | null, isLocked: boolean, unlocksAt: Date | null, lastResult: ResultSummary | null }`

---

**FR-102: Подборки по категориям (темам)**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как пользователь, я хочу играть в подборки по конкретным темам (наука, спорт, история), чтобы проверять знания в интересных мне областях.
- Описание: На главном экране секция с горизонтальным скроллом категорий. Каждая категория — карточка с иконкой, названием и бейджем с количеством доступных вопросов. При нажатии — генерируется набор из 10 случайных одобренных вопросов этой категории (с учётом антиповтора). После прохождения — результаты и возможность сыграть снова.
- Критерии приёмки:
  - AC-1: Когда пользователь видит секцию «Темы», тогда отображаются все активные категории
  - AC-2: Когда пользователь нажимает на категорию, тогда загружается набор из 10 вопросов данной категории
  - AC-3: Когда вопросы загружены, тогда открывается экран игры (тот же Card Screen)
  - AC-4: Когда все вопросы пройдены, тогда показываются результаты
  - AC-5: Когда у категории < 10 доступных вопросов (с учётом антиповтора), тогда показывается бейдж «Пройдено!»
- Связанные экраны: Home Screen → Categories Section, Card Game Screen
- Требует backend: Да
- API:
  - `GET /api/v1/categories` — обновить, добавить поле `availableCount` (количество доступных вопросов для пользователя)
  - `POST /api/v1/collections/by-category` — body: `{ categoryId, count: 10 }` → возвращает набор вопросов

---

**FR-103: Подборки по сложности**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как пользователь, я хочу выбирать подборки по сложности, чтобы бросить себе вызов или расслабиться.
- Описание: Три карточки на главном экране: «Лёгкие» (difficulty 1-2), «Средние» (difficulty 3), «Сложные» (difficulty 4-5). При нажатии — генерируется набор из 10 вопросов указанной сложности. Визуально карточки имеют разные цвета (зелёный/жёлтый/красный) и иконки.
- Критерии приёмки:
  - AC-1: Когда пользователь видит секцию «По сложности», тогда отображаются 3 карточки с разными цветами
  - AC-2: Когда пользователь нажимает «Сложные», тогда загружаются 10 вопросов с difficulty 4-5
  - AC-3: Когда набор завершён, тогда показываются результаты
  - AC-4: Когда пользователь играет снова, тогда генерируются другие вопросы (антиповтор)
- Связанные экраны: Home Screen → Difficulty Section
- Требует backend: Да
- API:
  - `POST /api/v1/collections/by-difficulty` — body: `{ difficulty: 'easy' | 'medium' | 'hard', count: 10 }` → набор вопросов

---

**FR-104: Фильтрация подборок на главном экране**
- Приоритет: Should
- Релиз: v1.1
- User Story: Как пользователь, я хочу фильтровать подборки по темам и сложности, чтобы быстро найти интересное.
- Описание: В верхней части экрана (под заголовком) — строка фильтров-чипов. Чипы: «Все», затем категории (Наука, Спорт, История...). При выборе чипа — секции обновляются, показывая только подборки выбранной категории. Дополнительно: фильтр по сложности (кнопки Easy/Medium/Hard).
- Критерии приёмки:
  - AC-1: Когда пользователь выбирает чип «Наука», тогда показываются только подборки с вопросами по науке
  - AC-2: Когда пользователь выбирает «Все», тогда показываются все подборки
  - AC-3: Когда фильтр применён, тогда секции «По сложности» и «Подборки» фильтруются соответственно
  - AC-4: Когда активен фильтр, тогда чип визуально выделен (filled)
- Связанные экраны: Home Screen → Filter Bar
- Зависимости: FR-100
- Требует backend: Нет (фильтрация на клиенте)

---

**FR-105: Специальные подборки (кураторские)**
- Приоритет: Should
- Релиз: v1.1
- User Story: Как пользователь, я хочу видеть специальные тематические подборки (например, «Мифы о здоровье»), чтобы изучать интересные узкие темы.
- Описание: Админ может создать именованную подборку из пула одобренных вопросов с заголовком, описанием, иконкой/изображением. Подборка отображается на главном экране в секции «Рекомендуемые». Может быть привязана к дате (сезонные: «Новогодние факты», «Олимпиада-2026»).
- Критерии приёмки:
  - AC-1: Когда админ создал и опубликовал подборку, тогда она появляется на главном экране
  - AC-2: Когда пользователь нажимает на подборку, тогда видит описание и кнопку «Начать»
  - AC-3: Когда подборка имеет даты действия, тогда она показывается только в этот период
  - AC-4: Когда подборка пройдена, тогда показываются результаты и бейдж «Пройдено»
- Связанные экраны: Home Screen → Featured Collections, Collection Detail Screen
- Требует backend: Да
- API:
  - `GET /api/v1/collections` — список доступных подборок (публичный)
  - `GET /api/v1/collections/:id` — детали подборки с вопросами

---

### 1.4 Функциональные требования — Админ-панель (подборки)

---

**FR-110: Управление категориями в админке (расширение)**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как владелец, я хочу создавать и редактировать категории, чтобы организовывать контент.
- Описание: Расширить существующий CRUD категорий:
  - Добавить поле `description` / `descriptionEn` (описание категории для пользователей)
  - Добавить поле `color` (цвет для карточки на главном экране, hex)
  - Добавить поле `imageUrl` (опциональное изображение/иконка)
  - Показывать количество одобренных вопросов в каждой категории
  - Сортировка drag-and-drop или по полю `sortOrder`
- Критерии приёмки:
  - AC-1: Когда владелец создаёт категорию, тогда задаёт: имя RU, имя EN, slug, иконка, цвет, описание, сортировка
  - AC-2: Когда владелец редактирует категорию, тогда изменения отражаются в мобильном приложении
  - AC-3: Когда владелец отключает категорию (isActive=false), тогда она не показывается в приложении
  - AC-4: Когда в таблице категорий, тогда видно количество одобренных вопросов
- Связанные экраны: Admin → Categories (обновлённый)
- Требует backend: Да

---

**FR-111: Создание специальных подборок в админке**
- Приоритет: Should
- Релиз: v1.1
- User Story: Как владелец, я хочу создавать тематические подборки вопросов с названием и описанием, чтобы предлагать пользователям интересный кураторский контент.
- Описание: Новая секция в админке «Подборки» (Collections). Форма создания:
  - Название (RU + EN)
  - Описание (RU + EN)
  - Иконка/эмодзи
  - Изображение (опционально, загрузка или AI-генерация)
  - Выбор вопросов из пула одобренных (10-20 штук)
  - Даты действия (опционально: startDate, endDate)
  - Статус: draft / published
  - Тип: `featured` (рекомендуемая) / `seasonal` (сезонная) / `thematic` (тематическая)
- Критерии приёмки:
  - AC-1: Когда владелец создаёт подборку, тогда задаёт все поля и выбирает вопросы
  - AC-2: Когда подборка опубликована, тогда она появляется в мобильном приложении
  - AC-3: Когда подборка имеет даты, тогда она автоматически скрывается после endDate
  - AC-4: Когда владелец редактирует подборку, тогда изменения применяются сразу
- Связанные экраны: Admin → Collections → List / Create / Edit
- Требует backend: Да
- API (Admin):
  - `GET /api/v1/admin/collections` — список подборок
  - `POST /api/v1/admin/collections` — создать
  - `PATCH /api/v1/admin/collections/:id` — обновить
  - `DELETE /api/v1/admin/collections/:id` — удалить

---

### 1.5 Модели данных — Новые/обновлённые сущности

```typescript
// Обновление Category
interface Category {
  id: string;
  name: string;
  nameEn: string;
  slug: string;
  icon: string;
  color: string;           // NEW: hex-цвет (#FF5722)
  description: string;     // NEW: описание RU
  descriptionEn: string;   // NEW: описание EN
  imageUrl: string | null;  // NEW: URL картинки
  isActive: boolean;
  sortOrder: number;
  questionsCount: number;  // COMPUTED: количество одобренных вопросов
  createdAt: Date;
}

// Новая сущность: Collection (подборка)
// Хранение: Remote (PostgreSQL)
interface Collection {
  id: string;
  title: string;           // Название RU
  titleEn: string;         // Название EN
  description: string;     // Описание RU
  descriptionEn: string;   // Описание EN
  icon: string;            // Эмодзи или иконка
  imageUrl: string | null;  // URL картинки обложки
  type: 'featured' | 'seasonal' | 'thematic';
  status: 'draft' | 'published';
  questionIds: string[];   // Привязанные вопросы (10-20)
  startDate: Date | null;  // Начало показа (null = всегда)
  endDate: Date | null;    // Конец показа (null = всегда)
  sortOrder: number;
  createdAt: Date;
  updatedAt: Date;
}

// Обновление UserQuestionHistory — для трекинга по типу подборки
interface UserCollectionProgress {
  id: string;
  userId: string;
  collectionType: 'daily' | 'category' | 'difficulty' | 'collection';
  collectionId: string | null;  // ID подборки (для collection type)
  categoryId: string | null;    // ID категории (для category type)
  difficulty: string | null;    // easy/medium/hard (для difficulty type)
  correctAnswers: number;
  totalQuestions: number;
  completedAt: Date;
}
```

### 1.6 Prisma Schema — Дополнения

```prisma
model Category {
  id            String     @id @default(cuid())
  name          String
  nameEn        String
  slug          String     @unique
  icon          String
  color         String     @default("#58CC02")  // NEW
  description   String     @default("")          // NEW
  descriptionEn String     @default("")          // NEW
  imageUrl      String?                          // NEW
  isActive      Boolean    @default(true)
  sortOrder     Int        @default(0)
  createdAt     DateTime   @default(now())

  questions     Question[]
}

model Collection {
  id            String    @id @default(cuid())
  title         String
  titleEn       String
  description   String    @default("")
  descriptionEn String    @default("")
  icon          String    @default("📚")
  imageUrl      String?
  type          String    @default("thematic")  // 'featured' | 'seasonal' | 'thematic'
  status        String    @default("draft")     // 'draft' | 'published'
  startDate     DateTime? @db.Date
  endDate       DateTime? @db.Date
  sortOrder     Int       @default(0)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  questions     CollectionQuestion[]
}

model CollectionQuestion {
  id           String     @id @default(cuid())
  collectionId String
  questionId   String
  sortOrder    Int

  collection   Collection @relation(fields: [collectionId], references: [id], onDelete: Cascade)
  question     Question   @relation(fields: [questionId], references: [id])

  @@unique([collectionId, questionId])
}

model UserCollectionProgress {
  id              String   @id @default(cuid())
  userId          String
  collectionType  String   // 'daily' | 'category' | 'difficulty' | 'collection'
  referenceId     String?  // collectionId, categoryId, или dailySetId
  difficulty      String?  // 'easy' | 'medium' | 'hard'
  correctAnswers  Int
  totalQuestions  Int
  completedAt     DateTime @default(now())

  user            User     @relation(fields: [userId], references: [id])

  @@index([userId, collectionType])
  @@index([userId, referenceId])
}
```

### 1.7 API Endpoints — Новые

```
# Mobile API — Подборки

GET    /api/v1/home/feed              — главный экран: все секции одним запросом
  Response: {
    daily: { set: DailySet | null, isLocked: boolean, unlocksAt: Date | null, lastResult: {...} | null },
    categories: [{ id, name, nameEn, slug, icon, color, availableCount }],
    collections: [{ id, title, titleEn, description, icon, imageUrl, type, questionCount }],
    userProgress: { dailyCompleted: boolean, streak: number }
  }

POST   /api/v1/collections/start      — начать подборку (генерирует набор вопросов)
  Request: { type: 'category' | 'difficulty' | 'collection', categoryId?, difficulty?, collectionId?, count: 10 }
  Response: { sessionId: string, questions: Question[] }

POST   /api/v1/collections/:sessionId/submit  — отправить результаты подборки
  Request: { results: [{ questionId, userAnswer, timeSpentSeconds }] }
  Response: { correctAnswers, totalQuestions, score, streak }

# Admin API — Подборки

GET    /api/v1/admin/collections       — список подборок (пагинация, фильтры)
GET    /api/v1/admin/collections/:id   — одна подборка
POST   /api/v1/admin/collections       — создать подборку
PATCH  /api/v1/admin/collections/:id   — обновить
DELETE /api/v1/admin/collections/:id   — удалить
```

### 1.8 User Flow — Обновлённый главный экран

```
[Home Screen — обновлённый]
  ├── [Header: "Fact or Fake" + StreakBadge]
  ├── [Filter Chips: Все | Наука | Спорт | История | ...]
  │
  ├── [Секция: Daily]
  │   └── [Выделенная карточка]
  │       ├── Если доступна: тема + «Начать» (15 вопросов)
  │       ├── Если пройдена: результат + «Новая через X дн.»
  │       └── Если заблокирована: таймер обратного отсчёта
  │
  ├── [Секция: Темы — горизонтальный скролл]
  │   ├── 🔬 Наука (47)
  │   ├── ⚽ Спорт (32)
  │   ├── 📜 История (56)
  │   ├── 🌍 География (28)
  │   ├── 🚀 Космос (19)
  │   ├── 🎨 Культура (35)
  │   └── ... (остальные категории)
  │   └── Тап → [Game Screen: 10 вопросов по теме]
  │
  ├── [Секция: По сложности]
  │   ├── 🟢 Лёгкие — расслабься
  │   ├── 🟡 Средние — проверь знания
  │   └── 🔴 Сложные — для экспертов
  │   └── Тап → [Game Screen: 10 вопросов по сложности]
  │
  ├── [Секция: Подборки — горизонтальный скролл карточек]
  │   ├── «Мифы о здоровье» (12 вопросов) — NEW
  │   ├── «Космические рекорды» (10 вопросов) — NEW
  │   └── «Странные законы» (15 вопросов) — NEW
  │   └── Тап → [Collection Detail → Game Screen]
  │
  └── [Ad Banner]
```

---

## 2. РЕДИЗАЙН UI — APPLE DESIGN

### 2.1 Концепция

**Текущее состояние:** UI имеет Duolingo-подобный стиль с толстыми тенями, яркими кнопками с эффектом «нажатия» (depth), и насыщенной палитрой. Пользователь описал текущий вид как «чопорный» (stuffy/rigid).

**Новое направление:** Apple Human Interface Guidelines — чистый, воздушный, минималистичный дизайн с акцентом на контент, а не на декоративные элементы.

### 2.2 Принципы Apple Design для приложения

| Принцип | Текущее | Новое |
|---------|---------|-------|
| **Пространство** | Плотная компоновка, мало воздуха | Щедрые отступы (16-24px), «дышащий» layout |
| **Тени** | Тяжёлые тени (duoShadow), эффект глубины на кнопках | Минимальные тени (0-2px blur), или без теней — разделение через цвет фона |
| **Кнопки** | 3D-эффект с depth (4px нижняя граница) | Плоские кнопки с заливкой или outline, мягкие скругления |
| **Карточки** | Card с тенями и рамками | Карточки с лёгким фоном (surface), без заметных теней, скругления 12-16px |
| **Типографика** | Nunito (декоративный, округлый) | SF Pro Display / Inter (системный, чистый). Если Nunito — использовать сдержаннее |
| **Цвета** | Яркий зелёный #58CC02, насыщенные акценты | Приглушённые пастельные тона, основной цвет чуть менее кричащий, фоны нейтральные |
| **Анимации** | Rotation, spring bounce | Плавные переходы, subtle fade/scale, iOS-стиль spring |
| **Навигация** | Bottom Tabs (уже хорошо) | Сохранить Bottom Tabs, добавить Large Title style в заголовках |
| **Иконки** | Feather icons | SF Symbols-стиль (более толстые stroke, filled варианты) |
| **Модалки** | Slide from bottom (уже хорошо) | Sheet-стиль с grab handle (как iOS sheet) |

### 2.3 Функциональные требования — UI

---

**FR-200: Редизайн мобильного приложения в стиле Apple HIG**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как пользователь, я хочу видеть современный, чистый и удобный интерфейс, чтобы получать удовольствие от использования приложения.
- Описание: Полный редизайн UI всех экранов с соблюдением Apple HIG:
  1. **Large Title Headers** — крупные заголовки (34pt) которые уменьшаются при скролле
  2. **Карточки** — лёгкие, без тяжёлых теней, фон чуть отличается от основного (#F2F2F7 для light)
  3. **Кнопки** — плоские с заливкой, без 3D-эффекта (убрать PRESS_DEPTH)
  4. **Цветовая палитра** — пересмотреть, приглушить яркость, добавить systemBlue/systemGreen в стиле iOS
  5. **Отступы** — увеличить padding карточек (16→20px), spacing между секциями (16→24px)
  6. **Шрифт** — сохранить Nunito но пересмотреть weights (меньше ExtraBold, больше SemiBold/Regular)
  7. **Иконки** — перейти на более «толстые» filled-варианты
  8. **Bottom Sheet** — модалки с grab-handle вверху (pill-индикатор)
  9. **Segmented Controls** — вместо Chip-фильтров использовать сегментированный контрол (для вкладок лидерборда и т.д.)
  10. **List Styles** — grouped/inset стиль для настроек (как iOS Settings)
- Критерии приёмки:
  - AC-1: Когда пользователь использует приложение, тогда UI воспринимается как «чистый» и «воздушный»
  - AC-2: Когда кнопки нажимаются, тогда отклик — плавный opacity/scale, без «проваливания»
  - AC-3: Когда скроллится контент, тогда Large Title уменьшается до обычного
  - AC-4: Когда открывается модалка, тогда появляется sheet с grab handle
  - AC-5: Когда применена тёмная тема, тогда используются системные тёмные цвета iOS (#1C1C1E фон, #2C2C2E карточки)
- Связанные экраны: ВСЕ экраны
- Зависимости: —
- Требует backend: Нет

---

**FR-201: Редизайн админ-панели в стиле Apple**
- Приоритет: Must
- Релиз: v1.1
- User Story: Как владелец, я хочу работать в красивой и удобной админке, которая не выглядит устаревшей.
- Описание: Редизайн веб-админки с акцентом на:
  1. **Sidebar** — чистый, с лёгким фоном, иконки + текст, выделение активного пункта без кричащих цветов
  2. **Таблицы** — Apple-стиль: лёгкие горизонтальные линии, hover-эффект, скруглённые углы контейнера
  3. **Формы** — просторные поля, группировка в секции, чёткие labels
  4. **Дашборд** — карточки с метриками в стиле Apple Health / macOS System Monitor
  5. **Цвета** — нейтральные серые фоны, акцентный systemBlue (#007AFF), systemGreen для success
  6. **Типографика** — Inter или SF Pro (если доступен), вес: Regular для текста, Semibold для заголовков
  7. **Кнопки** — плоские, с закругленными углами (8px), без теней
  8. **Dialogs** — центрированные, с blur-фоном, скруглённые
- Критерии приёмки:
  - AC-1: Когда владелец открывает админку, тогда UI чистый и современный
  - AC-2: Когда таблицы отображаются, тогда данные легко читаются с корректными отступами
  - AC-3: Когда формы заполняются, тогда поля группированы логично с достаточным пространством
  - AC-4: Когда sidebar активен, тогда выделение пункта — subtle, не кричащее
- Связанные экраны: ВСЕ экраны админки
- Зависимости: —
- Требует backend: Нет

---

### 2.4 Цветовая палитра — обновлённая (Apple-inspired)

**Light Theme:**
```
Background:         #F2F2F7  (systemGroupedBackground)
Surface (cards):    #FFFFFF  (secondarySystemGroupedBackground)
Surface Secondary:  #F2F2F7  (tertiarySystemGroupedBackground)

Primary:            #34C759  (systemGreen — чуть приглушённее текущего #58CC02)
Accent Blue:        #007AFF  (systemBlue)
Orange:             #FF9500  (systemOrange)
Red:                #FF3B30  (systemRed)
Yellow:             #FFCC00  (systemYellow)
Purple:             #AF52DE  (systemPurple)

Text Primary:       #000000  (label)
Text Secondary:     #3C3C43 99  (secondaryLabel, 60% opacity)
Text Tertiary:      #3C3C43 4D  (tertiaryLabel, 30% opacity)

Separator:          #3C3C43 4D  (separator, 29% opacity)
```

**Dark Theme:**
```
Background:         #000000  (systemBackground)
Surface (cards):    #1C1C1E  (secondarySystemBackground)
Surface Secondary:  #2C2C2E  (tertiarySystemBackground)

Primary:            #30D158  (systemGreen dark)
Accent Blue:        #0A84FF  (systemBlue dark)
Orange:             #FF9F0A  (systemOrange dark)
Red:                #FF453A  (systemRed dark)

Text Primary:       #FFFFFF
Text Secondary:     #EBEBF5 99  (60% opacity)
Separator:          #545458 99  (60% opacity)
```

### 2.5 Типографика — обновлённая

```
Мобильное приложение:
  Large Title:    34px / Nunito 700 (Bold, не ExtraBold)
  Title 1:        28px / Nunito 700
  Title 2:        22px / Nunito 600 (SemiBold)
  Title 3:        20px / Nunito 600
  Headline:       17px / Nunito 600
  Body:           17px / Nunito 400 (Regular)
  Callout:        16px / Nunito 400
  Subheadline:    15px / Nunito 400
  Footnote:       13px / Nunito 400
  Caption 1:      12px / Nunito 400
  Caption 2:      11px / Nunito 400

Админ-панель:
  Page Title:     28px / Inter 600
  Section Title:  20px / Inter 600
  Body:           14px / Inter 400
  Label:          13px / Inter 500
  Caption:        12px / Inter 400
```

---

## 3. ИСПРАВЛЕНИЕ БАГОВ API

### 3.1 Обнаруженные дефекты

После аудита серверного кода обнаружены следующие дефекты, объясняющие проблемы с Activity, Ranking и Streak.

---

**BUG-001: Activity секция пуста на экране профиля**
- Приоритет: Critical
- Компонент: Server → UsersService → getUserStats()
- Файл: `server/src/modules/users/users.service.ts`
- Проблема: Activity map формируется из `LeaderboardEntry.createdAt`, но:
  1. Используется `toISOString().split('T')[0]` — не учитывает timezone пользователя
  2. Данные берутся из таблицы `LeaderboardEntry`, которая заполняется только при отправке Daily Set. Если пользователь играл в категории или сложность — записей нет
  3. При вызове `toISOString()` дата конвертируется в UTC, что может давать неверную дату для пользователей в часовых поясах UTC+3 и далее
- Ожидаемое поведение: Activity map показывает ВСЕ дни, когда пользователь играл (любой тип подборки)
- Решение:
  - Использовать `DailySet.date` (тип `@db.Date`, без time-компонента) для daily-set записей
  - Добавить данные из `UserQuestionHistory.answeredAt` для любых других типов игр
  - Или использовать новую таблицу `UserCollectionProgress.completedAt`
  - Нормализовать timezone: принимать timezone пользователя или использовать фиксированный UTC+3

---

**BUG-002: Ranking (лидерборд) — некорректное ранжирование**
- Приоритет: Critical
- Компонент: Server → LeaderboardService
- Файл: `server/src/modules/leaderboard/leaderboard.service.ts`
- Проблемы:
  1. **Нет tiebreaker:** Два пользователя с одинаковым количеством правильных ответов получают разные ранги (зависит от порядка из БД)
  2. **Rank по индексу:** `rank: index + 1` — предполагает что `groupBy` сохраняет сортировку, что не гарантировано
  3. **Позиция пользователя:** Рассчитывается отдельным запросом и может не совпадать с позицией в основном списке
- Ожидаемое поведение: Корректный ранг с tiebreaker по `totalTimeSeconds` (меньше время = лучше). Позиция пользователя = его место в полном списке
- Решение:
  - Сортировать в памяти с tiebreaker: `correctAnswers DESC, totalTimeSeconds ASC`
  - Рассчитывать позицию пользователя из того же отсортированного массива

---

**BUG-003: Streak не обновляется**
- Приоритет: Critical
- Компонент: Server → DailySetsService → submitDailySet()
- Файл: `server/src/modules/daily-sets/daily-sets.service.ts`
- Проблемы:
  1. **Math.round вместо Math.floor:** При расчёте `diffDays = Math.round(diffTime / (1000 * 60 * 60 * 24))` — на границе суток `Math.round` может округлить 0.99 до 1, вызывая ложный «already played today»
  2. **totalGamesPlayed и totalCorrectAnswers НЕ обновляются** в `submitDailySet()` — обновляются только в `answerQuestion()` (отдельный endpoint). Если пользователь играет только через Daily Set — его stats остаются на 0
  3. **lastPlayedDate:** Устанавливается в `new Date()` (UTC), а сравнение с предыдущей датой не учитывает timezone
- Ожидаемое поведение: Стрик корректно инкрементируется при прохождении Daily Set. Stats обновляются. Timezone учитывается
- Решение:
  - Заменить `Math.round` на `Math.floor`
  - Добавить `totalGamesPlayed: { increment: 1 }` и `totalCorrectAnswers: { increment: correctAnswers }` в update транзакции
  - Нормализовать даты к UTC+3 (или пользовательскому timezone) перед сравнением

---

**BUG-004: Percentile рассчитывается некорректно**
- Приоритет: Medium
- Компонент: Server → DailySetsService → submitDailySet()
- Файл: `server/src/modules/daily-sets/daily-sets.service.ts`
- Проблема: Формула `(lowerCount / (totalPlayersToday - 1)) * 100` некорректна:
  - Если пользователь первый (lowerCount=0, total=1): `0 / 0 = NaN` (ловится fallback = 100, но случайно)
  - Если пользователь лучший из 100: `0 / 99 * 100 = 0%` — должно быть ~99%
- Ожидаемое поведение: Percentile = % игроков, которых пользователь обошёл
- Решение: `percentile = Math.round((lowerCount / totalPlayersToday) * 100)` или стандартная формула `((rank - 0.5) / total) * 100`

---

**BUG-005: Fallback вопросы не случайные**
- Приоритет: Low
- Компонент: Server → DailySetsService → getTodaySet()
- Файл: `server/src/modules/daily-sets/daily-sets.service.ts`
- Проблема: Когда нет опубликованного Daily Set, fallback берёт `take: 15, orderBy: createdAt DESC` — всегда одни и те же последние 15 вопросов
- Решение: Использовать random offset или `ORDER BY RANDOM()`

---

### 3.2 Функциональные требования — Исправления

---

**FR-300: Исправить Activity Map**
- Приоритет: Must
- Релиз: v1.1 (hotfix)
- Описание: Activity map должен показывать все дни активности пользователя: прохождение Daily Set, категорий, подборок по сложности, специальных подборок. Использовать дату DailySet для daily-записей и дату из UserQuestionHistory для остальных. Нормализовать к UTC+3.
- Критерии приёмки:
  - AC-1: Когда пользователь прошёл Daily Set 5 февраля, тогда на heatmap 5 февраля зелёный
  - AC-2: Когда пользователь играл в подборку по категории 6 февраля, тогда 6 февраля тоже зелёный
  - AC-3: Когда пользователь в часовом поясе UTC+3, тогда дата корректна (не ±1 день)

---

**FR-301: Исправить Leaderboard Ranking**
- Приоритет: Must
- Релиз: v1.1 (hotfix)
- Описание: Лидерборд должен корректно ранжировать пользователей по `correctAnswers DESC` с tiebreaker `totalTimeSeconds ASC`. Позиция текущего пользователя рассчитывается из того же набора данных.
- Критерии приёмки:
  - AC-1: Когда два пользователя имеют одинаковое количество правильных ответов, тогда выше тот, кто отвечал быстрее
  - AC-2: Когда пользователь открывает лидерборд, тогда его позиция совпадает с позицией в полном рейтинге
  - AC-3: Когда в лидерборде 0 записей, тогда показывается пустое состояние

---

**FR-302: Исправить Streak**
- Приоритет: Must
- Релиз: v1.1 (hotfix)
- Описание: Стрик должен корректно инкрементироваться при прохождении Daily Set. `totalGamesPlayed` и `totalCorrectAnswers` должны обновляться. Даты сравниваются корректно (UTC+3).
- Критерии приёмки:
  - AC-1: Когда пользователь играет два дня подряд (вчера в 23:59, сегодня в 00:01), тогда стрик = 2
  - AC-2: Когда пользователь завершает Daily Set, тогда `totalGamesPlayed += 1` и `totalCorrectAnswers += N`
  - AC-3: Когда пользователь пропустил день, тогда стрик сбрасывается до 0
  - AC-4: Когда стрик обновлён, тогда на Home Screen значение актуально без перезапуска

---

**FR-303: Исправить Percentile**
- Приоритет: Should
- Релиз: v1.1
- Описание: Percentile рассчитывается как `Math.round((lowerCount / totalPlayersToday) * 100)`.
- Критерии приёмки:
  - AC-1: Когда пользователь лучший из 100, тогда percentile = 99%
  - AC-2: Когда пользователь единственный, тогда percentile = 100%
  - AC-3: Когда пользователь в середине, тогда percentile корректен

---

## 4. ОБНОВЛЁННЫЕ USER FLOWS

### 4.1 Главный экран (с подборками)

```
[App Open]
  → [Home Screen]
    ├── Скролл вверх → Large Title уменьшается
    │
    ├── [Filter Chips: Все | Наука | Спорт | ...]
    │   └── Тап → секции фильтруются
    │
    ├── [Daily Card]
    │   ├── Доступна → Тап «Начать» → [Game Screen: 15 вопросов]
    │   │                              → [Results Screen]
    │   │                              → Daily Card: «Новая через 6 дн.»
    │   └── Заблокирована → Таймер, неактивна
    │
    ├── [Categories: горизонтальный скролл]
    │   └── Тап «Наука» → [Game Screen: 10 вопросов по науке]
    │                    → [Results Screen]
    │
    ├── [Difficulty: 3 карточки]
    │   └── Тап «Сложные» → [Game Screen: 10 сложных вопросов]
    │                      → [Results Screen]
    │
    └── [Featured Collections: горизонтальный скролл]
        └── Тап → [Collection Detail: описание + «Начать»]
                → [Game Screen: 10-20 вопросов]
                → [Results Screen]
```

### 4.2 Прохождение подборки по категории

```
[Home Screen] → Тап на «⚽ Спорт (32)»
  → POST /api/v1/collections/start { type: 'category', categoryId: 'xxx', count: 10 }
  → [Game Screen: 10 вопросов]
    → [Card 1] → свайп → [Explanation] → «Далее»
    → ... (cards 2-10)
    → [Ad Interstitial — после 5-й карточки]
    → ... (cards 6-10)
  → POST /api/v1/collections/:sessionId/submit
  → [Results Screen: 8/10 (80%)]
    → «Ещё раз» / «На главный»
  → [Home Screen: availableCount обновился]
```

---

## 5. ОБНОВЛЁННОЕ РЕЗЮМЕ ДЛЯ ДИЗАЙНЕРА

### Обновлённые экраны мобильного приложения

| # | Экран | Описание | Изменения |
|---|-------|---------|-----------|
| 1 | Splash Screen | Лого + загрузка | Минимальный редизайн (Apple-цвета) |
| 2-4 | Onboarding Steps 1-3 | Ценность, механика, язык | Редизайн: пространство, типографика |
| **5** | **Home Screen** | **Витрина подборок: Daily + Темы + Сложность + Подборки + Фильтры** | **ПОЛНОСТЬЮ НОВЫЙ LAYOUT** |
| 6 | Card Game Screen | Карточка с утверждением, свайп | Визуальный редизайн (Apple-style карточки) |
| 7 | Explanation Card | Объяснение + источник | Редизайн: Sheet-стиль, grab handle |
| 8 | Results Screen | Результаты набора | Редизайн + поддержка разных типов подборок |
| **NEW** | **Collection Detail** | **Описание подборки + «Начать»** | **НОВЫЙ ЭКРАН** |
| 9 | Profile / Stats Screen | Статистика, heatmap | Редизайн Apple-стиль, исправленный Activity |
| 10 | Leaderboard Screen | Рейтинг (4 периода) | Редизайн + Segmented Control + исправленный Ranking |
| 11 | Settings Screen | Настройки | Редизайн: iOS Grouped List стиль |
| 12 | Nickname Modal | Ввод никнейма | Sheet-стиль |
| 13 | Streak Milestone | Поздравление | Обновлённые анимации |

### Обновлённые экраны админ-панели

| # | Экран | Описание | Изменения |
|---|-------|---------|-----------|
| 1 | Login | Email + пароль | Редизайн Apple-стиль |
| 2 | Dashboard | Метрики | Карточки в стиле Apple Health |
| 3 | Questions: List | Таблица + фильтры | Редизайн таблиц (Apple-стиль) |
| 4 | Questions: Details | Просмотр/редактирование | Редизайн форм |
| 5 | Questions: Create | Ручное создание | Редизайн форм |
| 6 | Questions: Generate | AI-генерация | Редизайн |
| 7 | Daily Sets: List | Список наборов | Редизайн |
| 8 | Daily Sets: Create | Создание набора | Редизайн |
| 9 | Categories | Управление категориями | Расширение полей + редизайн |
| **NEW** | **Collections: List** | **Список кураторских подборок** | **НОВЫЙ ЭКРАН** |
| **NEW** | **Collections: Create/Edit** | **Создание/редактирование подборки** | **НОВЫЙ ЭКРАН** |

---

## 6. ПРИОРИТИЗАЦИЯ РЕАЛИЗАЦИИ

### Порядок выполнения (рекомендуемый):

**Этап 1 — Hotfix (1-2 дня):**
1. FR-302: Исправить Streak (Math.floor + totalGamesPlayed update)
2. FR-300: Исправить Activity Map (timezone + data source)
3. FR-301: Исправить Leaderboard Ranking (tiebreaker + user position)
4. FR-303: Исправить Percentile

**Этап 2 — UI Redesign (3-5 дней):**
5. FR-200: Редизайн мобильного приложения (Apple HIG)
6. FR-201: Редизайн админ-панели

**Этап 3 — Collections System (5-7 дней):**
7. FR-110: Расширение категорий (новые поля)
8. FR-102: Подборки по категориям (backend + frontend)
9. FR-103: Подборки по сложности
10. FR-101: Daily с недельной блокировкой
11. FR-100: Обновлённый главный экран
12. FR-104: Фильтрация

**Этап 4 — Collections Advanced (3-5 дней):**
13. FR-111: Создание подборок в админке
14. FR-105: Специальные подборки (frontend)

---

## 7. АНАЛИТИЧЕСКИЕ СОБЫТИЯ — ДОПОЛНЕНИЯ

| Событие | Параметры | Когда трекается |
|---------|-----------|-----------------|
| `collection_start` | `type` (daily/category/difficulty/collection), `referenceId`, `questionCount` | Начало любой подборки |
| `collection_complete` | `type`, `referenceId`, `correctCount`, `totalTime` | Завершение подборки |
| `home_section_scroll` | `section` (categories/difficulty/collections) | Горизонтальный скролл секции |
| `home_filter_applied` | `categorySlug` | Применение фильтра |
| `daily_locked_viewed` | `daysUntilUnlock` | Просмотр заблокированной Daily |
| `collection_detail_viewed` | `collectionId` | Просмотр детали подборки |
