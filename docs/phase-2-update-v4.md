# Фаза 2: Бизнес-аналитик — Обновление требований v4

> **Дата:** 2026-02-24
> **Статус:** Новые функциональные требования (4 FR)
> **Контекст:** Доработки UX, retention-механик и визуальной вовлечённости

---

## FR-201: Автоматическая генерация имени и эмоджи-аватарки при первом входе

**Приоритет:** Must
**Релиз:** v1.1

### User Story
Как новый пользователь, я хочу получить забавное случайное имя и эмоджи-аватарку при первом входе, чтобы сразу чувствовать себя частью сообщества и не тратить время на выбор никнейма.

### Описание

При первой регистрации устройства (POST `/api/v1/auth/register`) сервер автоматически генерирует:

1. **Имя** в формате `{Прилагательное} {Животное}` — по аналогии с Google Sheets ("Anonymous Axolotl", "Дерзкий Капибара").
2. **Эмоджи-аватарка** — случайный эмоджи животного, привязанный к выбранному животному.

Пользователь может в любой момент изменить никнейм вручную через существующий модал.

### Списки для генерации

**Животные (с эмоджи):**

| Животное RU | Животное EN | Эмоджи |
|-------------|-------------|--------|
| Капибара | Capybara | 🦫 |
| Аксолотль | Axolotl | 🦎 |
| Панда | Panda | 🐼 |
| Лиса | Fox | 🦊 |
| Сова | Owl | 🦉 |
| Кот | Cat | 🐱 |
| Ёж | Hedgehog | 🦔 |
| Пингвин | Penguin | 🐧 |
| Коала | Koala | 🐨 |
| Ленивец | Sloth | 🦥 |
| Фламинго | Flamingo | 🦩 |
| Хамелеон | Chameleon | 🦎 |
| Осьминог | Octopus | 🐙 |
| Дельфин | Dolphin | 🐬 |
| Кролик | Rabbit | 🐰 |
| Единорог | Unicorn | 🦄 |
| Выдра | Otter | 🦦 |
| Тукан | Toucan | 🐦 |
| Жираф | Giraffe | 🦒 |
| Красная панда | Red Panda | 🐾 |

**Прилагательные:**

| RU | EN |
|----|-----|
| Дерзкий | Bold |
| Мудрый | Wise |
| Хитрый | Sly |
| Быстрый | Swift |
| Весёлый | Cheerful |
| Храбрый | Brave |
| Ловкий | Nimble |
| Загадочный | Mysterious |
| Сонный | Sleepy |
| Дикий | Wild |
| Космический | Cosmic |
| Эпичный | Epic |
| Тихий | Quiet |
| Магический | Magical |
| Отважный | Fearless |
| Пушистый | Fluffy |
| Нежный | Gentle |
| Яркий | Bright |
| Ленивый | Lazy |
| Бодрый | Lively |

### Критерии приёмки

- **AC-1:** При первом запуске (регистрации устройства) сервер генерирует `nickname` и `avatarEmoji` автоматически. Никнейм в формате `"{Adj} {Animal}"` на языке контента пользователя.
- **AC-2:** Эмоджи-аватарка соответствует выбранному животному из таблицы выше.
- **AC-3:** Если сгенерированный никнейм уже занят — добавить случайное двузначное число в конец (например, "Хитрый Кот 42").
- **AC-4:** Аватар (компонент `Avatar.tsx`) отображает эмоджи вместо инициалов, если поле `avatarEmoji` заполнено.
- **AC-5:** Пользователь по-прежнему может изменить никнейм через модал. При ручном изменении `avatarEmoji` сохраняется.
- **AC-6:** В лидерборде и профиле отображаются эмоджи-аватарка и имя.

### Изменения в данных

**Prisma User model — добавить поле:**
```prisma
avatarEmoji  String?   // Эмоджи-аватарка (🦊, 🐼, etc.)
```

**Серверная утилита:** `server/src/utils/generate-nickname.ts`
- Функция `generateNickname(language: 'ru' | 'en'): { nickname: string; avatarEmoji: string }`
- Используется при регистрации в `auth.service.ts`

**API изменения:**
- `POST /api/v1/auth/register` — response включает `nickname` и `avatarEmoji`
- `GET /api/v1/profile` — response включает `avatarEmoji`
- `PATCH /api/v1/profile` — можно обновить `avatarEmoji` (опционально)

### Связанные экраны
- Splash / первый запуск (автоматическая регистрация)
- Профиль (отображение)
- Лидерборд (отображение)
- Модал никнейма (редактирование)

### Зависимости: Нет
### Требует backend: Да

---

## FR-202: Фиксированная высота карточек категорий (Topics)

**Приоритет:** Must
**Релиз:** v1.1

### User Story
Как пользователь, я хочу чтобы карточки категорий на главном экране были одинаковой высоты, чтобы интерфейс выглядел аккуратно и не "прыгал" при смене фильтров.

### Описание

Карточки категорий в горизонтальном скролле на Home-экране должны иметь одинаковую фиксированную высоту, независимо от длины текста названия или количества доступных вопросов. Если текст не помещается — обрезать с `numberOfLines`.

### Критерии приёмки

- **AC-1:** Все карточки категорий в горизонтальном FlatList имеют одинаковую фиксированную высоту (140px).
- **AC-2:** Название категории обрезается до 2 строк максимум (`numberOfLines={2}`).
- **AC-3:** При переключении фильтров layout не "прыгает" — контейнер секции имеет фиксированную высоту.
- **AC-4:** Визуально: иконка сверху, название по центру, счётчик внизу — элементы выровнены с `justifyContent: 'space-between'`.

### Связанные экраны
- Home (`app/(tabs)/home/index.tsx`) — компонент `CategoryCard`

### Изменения в коде
- `CategoryCard`: добавить `height: 140`, `justifyContent: 'space-between'`
- Контейнер секции: уже имеет `height: 160` (включая padding)

### Зависимости: Нет
### Требует backend: Нет

---

## FR-203: Переработка логики стриков — стрик по правильным ответам подряд

**Приоритет:** Must
**Релиз:** v1.1

### User Story
Как пользователь, я хочу чтобы мой стрик отражал количество правильных ответов подряд (независимо от категории, сета или режима), чтобы видеть свой реальный прогресс и испытывать азарт от непрерывной серии.

### Описание

**Текущая логика (убрать):**
- Стрик считается по дням: "сколько дней подряд пользователь играл в daily set".
- Зависит от конкретного daily set и 14-дневного окна.
- Не учитывает правильность ответов.

**Новая логика:**
- **Стрик = количество правильных ответов подряд (across all sessions).**
- Каждый правильный ответ увеличивает `currentStreak` на 1.
- Любой неправильный ответ **сбрасывает** `currentStreak` в 0.
- Стрик считается глобально: daily set, категория, коллекция, difficulty — всё идёт в один стрик.
- `bestStreak` обновляется при каждом ответе: `bestStreak = max(bestStreak, currentStreak)`.

### Критерии приёмки

- **AC-1:** Правильный ответ на вопрос увеличивает `currentStreak` на 1.
- **AC-2:** Неправильный ответ сбрасывает `currentStreak` в 0.
- **AC-3:** Стрик работает одинаково для daily set, категорий, коллекций и difficulty-режимов.
- **AC-4:** `bestStreak` обновляется в реальном времени при каждом правильном ответе.
- **AC-5:** При смене сессии (завершил daily set → начал категорию) стрик **продолжается** с того же значения.
- **AC-6:** В UI (GameHeader, StreakBadge) стрик обновляется **после каждого ответа**, а не только в конце сета.
- **AC-7:** На экране результатов показывается текущий стрик и лучший стрик.
- **AC-8:** `lastPlayedDate` по-прежнему обновляется (для аналитики), но не влияет на стрик.

### Изменения в данных

**Prisma User model:**
- `currentStreak` — теперь означает "текущее кол-во правильных ответов подряд" (было: "дни подряд").
- `bestStreak` — семантика та же, шкала другая.
- Убрать зависимость от `lastPlayedDate` в расчёте стрика.

**API изменения:**
- `POST /api/v1/daily-sets/:id/submit` — в ответ добавить обновлённые `currentStreak` и `bestStreak`.
- `POST /api/v1/collections/submit` — аналогично.
- В обоих endpoint-ах: итерировать по каждому ответу, обновляя стрик инкрементально.

**Мобильный клиент:**
- `useCardGame.ts` — при каждом свайпе обновлять локальный стрик.
- `useUserStore.ts` — обновить `currentStreak` после каждого ответа.
- `GameHeader` / `StreakBadge` — показывать живой стрик во время игры.

### Миграция данных
- Существующие `currentStreak` и `bestStreak` сбросить в 0 при миграции.
- Альтернатива: пересчитать из `UserQuestionHistory` (если данные позволяют).

### Связанные экраны
- Game card (`app/game/card.tsx`)
- Results modal (`app/modal/results.tsx`)
- Profile (`app/(tabs)/profile/index.tsx`)
- Home — StreakBadge
- Streak milestone modal

### Зависимости: FR-204 зависит от этого FR
### Требует backend: Да

---

## FR-204: Анимация огня для стрика с градацией по порогам

**Приоритет:** Should
**Релиз:** v1.1

### User Story
Как пользователь, я хочу видеть нарастающую анимацию огня при увеличении стрика, чтобы испытывать удовлетворение от серии правильных ответов и мотивацию не прерывать серию.

### Описание

Компонент `StreakBadge` отображает анимированный огонь, интенсивность и цвет которого растут с текущим значением стрика. Анимация через `react-native-reanimated` (без Lottie).

### Пороги анимации

| Tier | Стрик | Цвет огня | Анимация | Визуальный эффект |
|------|-------|-----------|----------|-------------------|
| 0 | 0 | — | Нет | Бейдж серый, без огня |
| 1 | 1–4 | Amber `#F59E0B` | Минимальная | Маленький огонёк, покачивание ±3° |
| 2 | 5–9 | Оранжевый `#F97316` | Лёгкая | Огонь побольше, покачивание ±5°, лёгкий glow |
| 3 | 10–24 | Красно-оранжевый `#EF4444` | Средняя | Двойной огонь + отражение, пульсация scale 1.0→1.05, glow средний |
| 4 | 25–49 | Красно-фиолетовый `#DC2626`→`#9333EA` | Сильная | Тройной огонь, пульсация 1.0→1.08, яркий glow, мелкие частицы |
| 5 | 50–84 | Фиолетовый `#7C3AED` | Максимальная | Фиолетовый огонь, пульсация 1.0→1.1, мерцающий glow, частицы вверх |
| 6 | 85–99 | Тёмный фиолетовый `#6D28D9` | Эпичная | Полностью фиолетовый, halo, bounce-анимация числа |
| 7 | 100+ | Индиго-золотой `#6366F1`+`#EAB308` | Легендарная | Индиго-золотой огонь, радужный glow, корона над огнём, shimmer |

### Критерии приёмки

- **AC-1:** При стрике 0 — бейдж серый, без анимации.
- **AC-2:** При стрике 1–4 — amber огонёк с покачиванием.
- **AC-3:** При стрике 5+ — усиление: больший scale, дополнительные элементы.
- **AC-4:** При стрике 10+ — glow-эффект вокруг бейджа.
- **AC-5:** При стрике 25+ — particle-эффект (точки поднимаются вверх).
- **AC-6:** При стрике 50+ — цвет полностью фиолетовый.
- **AC-7:** При стрике 100+ — легендарная визуализация (золотые акценты, максимальный glow).
- **AC-8:** Переход между тирами анимирован: `interpolateColor` для цвета, `withSpring` для scale.
- **AC-9:** FPS ≥ 55 при любом тире анимации.
- **AC-10:** При правильном ответе — burst-эффект на огне (быстрый scale up → down).
- **AC-11:** При неправильном ответе (стрик → 0) — "затухание" огня (fade out + scale down за 300ms).

### Техническая реализация

**Файлы:**
- `mobile/src/features/game/components/StreakBadge.tsx` — полная переработка
- Возможно: `mobile/src/features/game/components/FireParticles.tsx` — частицы (tier 4+)

**Подход:**
1. `react-native-reanimated`: `useSharedValue`, `withRepeat`, `withSequence`, `interpolateColor`
2. Огонь: несколько `Animated.View` с `MaterialCommunityIcons` "fire" (разные размеры, прозрачности)
3. Glow: `shadowColor` + `shadowRadius` через `useAnimatedStyle`
4. Частицы: массив Animated.View (3–5 шт) с `withRepeat(withTiming(...))` вертикально + fade out
5. Утилита `getStreakTier(streak: number): 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7`

### Связанные экраны
- Game card (GameHeader) — живое обновление при каждом ответе
- Home (StreakBadge в хедере)
- Results modal
- Profile

### Зависимости: FR-203 (логика стриков)
### Требует backend: Нет

---

## Приоритет реализации

| # | FR | Описание | Сложность |
|---|-----|----------|-----------|
| 1 | FR-202 | Фиксированная высота карточек | Низкая (чисто UI) |
| 2 | FR-201 | Авто-никнейм + эмоджи | Средняя (server + mobile) |
| 3 | FR-203 | Новая логика стриков | Высокая (server + mobile, ломающее изменение) |
| 4 | FR-204 | Анимация огня | Средняя (чисто UI, зависит от FR-203) |

## Аналитические события

| Событие | Параметры | Когда |
|---------|-----------|-------|
| `nickname_auto_generated` | `language`, `nickname`, `animal` | При авто-генерации |
| `streak_increment` | `newStreak`, `tier` | При правильном ответе |
| `streak_reset` | `lostStreak`, `tier` | При неправильном ответе |
| `streak_tier_up` | `fromTier`, `toTier`, `streak` | При переходе на новый порог |
| `streak_best_updated` | `newBest`, `previousBest` | При обновлении лучшего стрика |
