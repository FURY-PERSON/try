# Фаза 2: Бизнес-аналитик — Обновление требований v6

> **Дата:** 2026-02-26
> **Статус:** Новые требования (10 задач: 6 багфиксов, 2 UI-улучшения, 2 контентные задачи)
> **Контекст:** Доработка админ-панели, исправление багов, наполнение контентом

---

## FR-210: Скрыть AI-генерацию из навигации

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** UI-изменение

### User Story
Как администратор, я не хочу видеть раздел AI-генерации в навигации, чтобы не отвлекаться на неиспользуемую функциональность.

### Описание
Полностью скрыть пункт навигации «AI Генерация» из бокового меню админ-панели. Страница `/questions/generate` остаётся доступной по прямой ссылке (не удалять роут), но из сайдбара убирается.

### Затрагиваемые файлы
- `web/src/components/layout/AppLayout.tsx` — удалить элемент `{ to: '/questions/generate', icon: Sparkles, label: 'AI Генерация' }` из массива `NAV_ITEMS` (строка 18)

### Критерии приёмки
- AC-1: В боковой навигации отсутствует пункт «AI Генерация»
- AC-2: Роут `/questions/generate` по-прежнему работает при прямом переходе
- AC-3: Порядок остальных пунктов не изменился

---

## FR-211: Поиск и фильтрация в секции «Одобренные утверждения» при создании ежедневного набора

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** UI-улучшение

### User Story
Как администратор, при создании ежедневного набора я хочу искать утверждения по тексту и фильтровать по статусу (факт/фейк) и категории, чтобы быстро находить нужные утверждения из большого списка.

### Описание
На странице `DailySetCreatePage` в секции «Одобренные утверждения» (правая панель) добавить:

1. **Поле поиска** — текстовый инпут с иконкой `Search` и плейсхолдером «Поиск по тексту...». Фильтрует утверждения по полю `statement` (клиентская фильтрация по подстроке, case-insensitive).

2. **Фильтр по статусу (факт/фейк)** — Select-компонент:
   - Все (значение по умолчанию)
   - Факты (`isTrue: true`)
   - Фейки (`isTrue: false`)

3. **Фильтр по категории** — Select-компонент с динамическим списком категорий:
   - Все категории (значение по умолчанию)
   - Список категорий из `api.admin.categories.list()`, отображать `category.name`

### Затрагиваемые файлы
- `web/src/pages/DailySetCreatePage.tsx` — основные изменения

### Текущее поведение
Секция «Одобренные утверждения» (строки 183-222) загружает все одобренные утверждения одним запросом `{ status: 'approved', limit: 100 }` и отображает их простым списком без поиска/фильтрации. При большом количестве утверждений неудобно находить нужные.

### Требуемое поведение

**Макет секции:**
```
┌──────────────────────────────────┐
│ Одобренные утверждения           │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ 🔍 Поиск по тексту...       │ │
│ └──────────────────────────────┘ │
│                                  │
│ ┌────────────┐ ┌───────────────┐ │
│ │ Все ▼      │ │ Все категории▼│ │
│ └────────────┘ └───────────────┘ │
│                                  │
│ [список утверждений]             │
└──────────────────────────────────┘
```

**Логика фильтрации (клиентская):**
```typescript
const filteredQuestions = approvedQuestions
  .filter((q) => !selectedQuestionIds.includes(q.id))
  .filter((q) => !searchQuery || q.statement.toLowerCase().includes(searchQuery.toLowerCase()))
  .filter((q) => !isTrueFilter || String(q.isTrue) === isTrueFilter)
  .filter((q) => !categoryFilter || q.categoryId === categoryFilter);
```

**Состояния:**
```typescript
const [searchQuery, setSearchQuery] = useState('');
const [isTrueFilter, setIsTrueFilter] = useState('');
const [categoryFilter, setCategoryFilter] = useState('');
```

**Дополнительный запрос категорий:**
```typescript
const { data: categoriesData } = useQuery({
  queryKey: ['admin', 'categories'],
  queryFn: () => api.admin.categories.list(),
});
const categories = categoriesData?.data.data ?? [];
```

### Критерии приёмки
- AC-1: Над списком утверждений есть поле поиска с иконкой и плейсхолдером
- AC-2: При вводе текста список фильтруется в реальном времени по тексту утверждения
- AC-3: Select «Все / Факты / Фейки» фильтрует по полю `isTrue`
- AC-4: Select с категориями фильтрует по `categoryId`
- AC-5: Все три фильтра работают совместно (AND-логика)
- AC-6: Показывается количество отфильтрованных результатов
- AC-7: При сбросе фильтров отображается полный список

---

## BUG-301: На странице категорий вместо иконок отображаются текстовые названия

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** Багфикс

### Описание проблемы
На странице «Категории» (`CategoriesPage`) в колонке «Иконка» вместо эмоджи отображаются текстовые строки (например, "flask", "scroll", "globe" и т.д.).

### Причина
В БД в поле `icon` категорий хранятся текстовые идентификаторы иконок (например, `"flask"`, `"scroll"`) вместо Unicode-эмоджи (например, `"🧪"`, `"📜"`).

Ячейка таблицы отображает значение `cat.icon` как есть:
```tsx
// CategoriesPage.tsx, строка 160
<TableCell className="text-2xl">{cat.icon}</TableCell>
```

### Варианты решения

**Вариант A (рекомендуемый): Обновить данные в БД**
Заменить текстовые идентификаторы на эмоджи непосредственно в записях категорий через админ-панель (редактирование каждой категории) или через seed/миграцию.

Маппинг для обновления:
| Текущее значение | Заменить на |
|-----------------|------------|
| flask | 🧪 |
| scroll | 📜 |
| globe | 🌍 |
| landmark | 🏛️ |
| cpu | 💻 |
| palette | 🎨 |
| dna | 🧬 |
| trophy | 🏆 |
| utensils | 🍴 |
| film | 🎬 |
| leaf | 🌿 |
| music | 🎵 |
| heart | ❤️ |
| rocket | 🚀 |
| book | 📖 |

**Вариант B: Рендер через маппинг (фоллбэк)**
Если часть категорий ещё использует текстовые идентификаторы, добавить fallback-маппинг:
```tsx
const ICON_MAP: Record<string, string> = {
  flask: '🧪', scroll: '📜', globe: '🌍', /* ... */
};

function renderIcon(icon: string) {
  // Если icon уже является эмоджи (не ASCII) — отобразить как есть
  if (/\p{Emoji}/u.test(icon)) return icon;
  return ICON_MAP[icon] ?? '❓';
}
```

### Критерии приёмки
- AC-1: В колонке «Иконка» на странице категорий отображаются эмоджи, а не текст
- AC-2: При редактировании категории в поле «Иконка (emoji)» отображается эмоджи
- AC-3: В мобильном приложении категории также отображаются с эмоджи

---

## BUG-302: Модальные окна открываются в левом верхнем углу вместо центра экрана

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** Багфикс

### Описание проблемы
Все модальные окна (Dialog) в админ-панели открываются в левом верхнем углу экрана вместо центра. Затронуты все страницы, использующие компонент `Dialog`: Категории, Справочники, Подборки.

### Причина
Компонент `Dialog` (`web/src/components/ui/Dialog.tsx`) использует нативный элемент `<dialog>` с `showModal()`. Стили центрирования применяются через CSS-класс `open:flex open:items-center open:justify-center`, но `<dialog>` при `showModal()` позиционируется браузером через `::backdrop` и встроенные стили, которые конфликтуют с flexbox.

### Текущий код (строки 28-36):
```tsx
<dialog
  ref={dialogRef}
  onClose={onClose}
  className={cn(
    'fixed inset-0 z-50 bg-transparent backdrop:bg-black/40',
    'open:flex open:items-center open:justify-center',
  )}
>
  <div className={cn('bg-surface rounded-2xl p-6 w-full max-w-lg shadow-xl', className)}>
```

### Решение
Заменить нативный `<dialog>` на div-based overlay с правильным центрированием:

```tsx
export function Dialog({ open, onClose, title, description, children, className }: DialogProps) {
  if (!open) return null;

  return (
    <>
      {/* Backdrop */}
      <div
        className="fixed inset-0 z-50 bg-black/40"
        onClick={onClose}
      />
      {/* Modal */}
      <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
        <div
          className={cn('bg-surface rounded-2xl p-6 w-full max-w-lg shadow-xl', className)}
          onClick={(e) => e.stopPropagation()}
        >
          <div className="flex items-center justify-between mb-4">
            <div>
              <h2 className="text-base font-semibold text-text-primary">{title}</h2>
              {description && (
                <p className="text-sm text-text-secondary mt-0.5">{description}</p>
              )}
            </div>
            <button
              onClick={onClose}
              className="p-1.5 rounded-lg text-text-secondary hover:bg-surface-secondary transition-colors"
            >
              <X className="w-4 h-4" />
            </button>
          </div>
          {children}
        </div>
      </div>
    </>
  );
}
```

### Затрагиваемые файлы
- `web/src/components/ui/Dialog.tsx` — единственный файл для исправления

### Критерии приёмки
- AC-1: Модальное окно открывается строго по центру экрана (горизонтально и вертикально)
- AC-2: Backdrop затемняет весь экран
- AC-3: Клик по backdrop закрывает модалку
- AC-4: Клик внутри модалки не закрывает её
- AC-5: Кнопка «X» закрывает модалку
- AC-6: Проверено на страницах: Категории, Справочники (все 3 вкладки), Подборки
- AC-7: Scroll основного контента заблокирован при открытой модалке

---

## FR-212: Emoji-пикер для полей ввода эмоджи

**Приоритет:** Should
**Релиз:** v1.2
**Тип:** UI-улучшение

### User Story
Как администратор, при нажатии на поле ввода эмоджи я хочу видеть выпадающий список эмоджи, чтобы выбирать иконку визуально, а не вводить/вставлять вручную.

### Описание
Создать переиспользуемый компонент `EmojiPicker`, который при клике на инпут открывает выпадающий список с эмоджи. Компонент используется на следующих страницах:

1. **CategoriesPage** — поле «Иконка (emoji)»
2. **CollectionsPage** — поле «Иконка» в форме создания/редактирования подборки
3. **ReferencePage (Animals tab)** — поле «Эмоджи»
4. **ReferencePage (Emojis tab)** — поле «Эмоджи»

### Реализация

**Вариант A (рекомендуемый): Компонент EmojiPickerInput**

Создать `web/src/components/ui/EmojiPickerInput.tsx`:
```tsx
type EmojiPickerInputProps = {
  value: string;
  onChange: (emoji: string) => void;
  label?: string;
  error?: string;
  placeholder?: string;
};
```

**Список эмоджи по категориям:**
- Животные: 🐶🐱🦊🐻🐼🐨🐯🦁🐮🐷🐸🐵🐔🐧🐦🦅🦆🦉🐺🐗🐴🦄🐝🐛🦋🐌🐞🐢🐍🦎🐙🦑🦐🦀🐡🐠🐟🐬🐳🦈
- Природа: 🌿🌸🌺🌻🌹🌷💐🍀🍁🍂🌴🌵🌾🌱🌲🌳🍄
- Еда: 🍎🍐🍊🍋🍌🍉🍇🍓🍑🍒🍍🥝🥭🥥🍔🍕🌭🌮🍣🍜
- Наука: 🧪🧬🔬🔭🧲🧫💊💉🩺
- Спорт: ⚽🏀🏈⚾🎾🏐🏉🏆🥇🏅
- Искусство: 🎨🎭🎪🎬🎤🎵🎶🎹🎸🎺
- Путешествия: 🌍🌎🌏🗺️🏔️🌋🗻🏝️✈️🚀
- Объекты: 📖📜📚✏️🔑💡⏰📷💻🖥️
- Символы: ❤️💛💚💙💜🖤💔⭐🌟✨💫🔥

**Поведение:**
- По клику на инпут — открывается dropdown с эмоджи
- Эмоджи сгруппированы по категориям с заголовками
- При выборе эмоджи — значение устанавливается в инпут, dropdown закрывается
- Можно вручную ввести/вставить эмоджи в инпут
- Dropdown закрывается при клике вне области

### Критерии приёмки
- AC-1: При клике на инпут эмоджи открывается выпадающий список с эмоджи
- AC-2: Эмоджи сгруппированы по категориям
- AC-3: При выборе эмоджи значение устанавливается и dropdown закрывается
- AC-4: Ручной ввод эмоджи по-прежнему работает
- AC-5: Компонент используется на всех 4 страницах

---

## BUG-303: Ошибка при открытии страницы «Справочники»

**Приоритет:** Critical
**Релиз:** v1.2
**Тип:** Багфикс

### Описание проблемы
При переходе на страницу «Справочники» (`/reference`) приложение падает с ошибкой (белый экран или красный overlay в dev-режиме).

### Диагностика
Необходимо воспроизвести ошибку и определить:
1. Тип ошибки (runtime JS error, network error, render error)
2. Stack trace из консоли браузера
3. Ответ API (если ошибка на бэкенде)

**Возможные причины:**
- Ошибка в API-эндпоинте `/admin/reference/*` (adjectives/animals/emojis)
- Неверная структура ответа (data unwrapping: `data?.data` vs `data?.data.data`)
- Отсутствие таблиц в БД (если миграции не применены)
- Ошибка в компоненте `ReferencePage` при обработке пустых данных

**Обратить внимание на:**
Компонент `AdjectivesTab` (строка 73): `const items = data?.data ?? [];`
Компонент `AnimalsTab` (строка 242): `const items = data?.data ?? [];`
Компонент `EmojisTab` (строка 414): `const items = data?.data ?? [];`

Проверить, что API-ответ имеет структуру `{ data: [...] }`, а не `{ data: { data: [...] } }`.

### Действия
1. Открыть `/reference` в браузере с открытой консолью DevTools
2. Зафиксировать ошибку из консоли и Network-вкладки
3. Исправить корневую причину
4. Проверить все 3 вкладки: Прилагательные, Животные, Эмоджи

### Критерии приёмки
- AC-1: Страница `/reference` открывается без ошибок
- AC-2: Все 3 вкладки (Прилагательные, Животные, Эмоджи) корректно отображают данные
- AC-3: CRUD-операции (создание, редактирование, удаление) работают на каждой вкладке
- AC-4: Переключение между вкладками работает без ошибок

---

## BUG-304: Подборки не отображаются в мобильном приложении

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** Багфикс

### Описание проблемы
В мобильном приложении на главном экране секция «Подборки» (Featured Collections) не отображается, хотя в админ-панели подборки созданы и опубликованы.

### Текущий код (mobile/app/(tabs)/home/index.tsx, строка 362-390):
```tsx
{collections.length > 0 && (
  <AnimatedEntrance delay={400}>
    ...
    <FlatList data={collections} ... />
  </AnimatedEntrance>
)}
```

Секция рендерится условно — только если `collections.length > 0`. Если массив пуст, секция не показывается.

### Возможные причины
1. **Нет опубликованных подборок** — в БД нет записей `Collection` со `status: 'published'`
2. **Неверные даты** — `startDate` в будущем или `endDate` в прошлом
3. **API возвращает пустой массив** — проверить `HomeService.getPublishedCollections()`
4. **Неправильный маппинг данных на клиенте** — проверить `feed?.collections`
5. **Подборки без вопросов** — коллекции существуют, но `questions[]` пуст

### Действия по диагностике
1. Проверить в админке: есть ли подборки со статусом «Опубликовано»?
2. Проверить API напрямую: `GET /api/v1/home/feed` — содержит ли поле `collections`?
3. Если подборок нет — создать (см. FR-213)
4. Если API возвращает пустой массив при наличии опубликованных подборок — исследовать фильтрацию по датам в `HomeService.getPublishedCollections()`

### Критерии приёмки
- AC-1: При наличии опубликованных подборок они отображаются на главном экране мобильного приложения
- AC-2: Карточки подборок показывают иконку, название, описание, количество вопросов
- AC-3: По нажатию на подборку открывается экран деталей

---

## FR-213: Создать и наполнить подборки вопросами

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** Контентная задача

### Описание
Через админ-панель создать минимум 2 подборки (Collections) и наполнить каждую вопросами из одобренных утверждений. После создания — опубликовать.

### Подборка 1: «Удивительная наука»
| Поле | Значение |
|------|---------|
| Название (RU) | Удивительная наука |
| Название (EN) | Amazing Science |
| Описание (RU) | Факты и мифы из мира науки, которые вас удивят |
| Описание (EN) | Science facts and myths that will surprise you |
| Иконка | 🔬 |
| Тип | featured |
| Порядок | 1 |
| Статус | published |

Наполнить 8-15 вопросами из категорий: Наука, Технологии, Природа (из одобренных утверждений).

### Подборка 2: «Вокруг света»
| Поле | Значение |
|------|---------|
| Название (RU) | Вокруг света |
| Название (EN) | Around the World |
| Описание (RU) | Проверьте свои знания о странах, городах и культурах |
| Описание (EN) | Test your knowledge about countries, cities and cultures |
| Иконка | 🌍 |
| Тип | featured |
| Порядок | 2 |
| Статус | published |

Наполнить 8-15 вопросами из категорий: География, История, Культура (из одобренных утверждений).

### Критерии приёмки
- AC-1: Создано минимум 2 подборки через админ-панель
- AC-2: Каждая подборка содержит не менее 8 одобренных вопросов
- AC-3: Обе подборки опубликованы (статус: published)
- AC-4: Подборки отображаются в мобильном приложении (после исправления BUG-304)

---

## FR-214: Переименовать раздел «Справочники» в «Никнеймы и аватары»

**Приоритет:** Should
**Релиз:** v1.2
**Тип:** UI-изменение

### User Story
Как администратор, я хочу, чтобы раздел «Справочники» назывался более понятно и отражал своё содержание — управление данными для генерации никнеймов и выбора аватаров.

### Описание
Текущее название «Справочники» слишком обобщённое и не передаёт назначение раздела. Раздел содержит 3 вкладки: Прилагательные, Животные, Эмоджи — всё это данные для системы персонализации (никнеймы + аватары).

### Изменения

| Место | Было | Стало |
|-------|------|-------|
| Навигация (AppLayout.tsx) | `label: 'Справочники'` | `label: 'Никнеймы и аватары'` |
| Заголовок страницы (ReferencePage.tsx) | `title="Справочники"` | `title="Никнеймы и аватары"` |
| Подзаголовок страницы | `description="Управление никнеймами и аватарами"` | `description="Прилагательные, животные и эмоджи для персонализации"` |

### Затрагиваемые файлы
- `web/src/components/layout/AppLayout.tsx` — строка 22, изменить `label`
- `web/src/pages/ReferencePage.tsx` — строка 582, изменить `title` и `description`

### Критерии приёмки
- AC-1: В боковой навигации отображается «Никнеймы и аватары» вместо «Справочники»
- AC-2: Заголовок страницы — «Никнеймы и аватары»
- AC-3: Подзаголовок — «Прилагательные, животные и эмоджи для персонализации»

---

## FR-215: Наполнить справочник данными (20 прилагательных + 20 животных)

**Приоритет:** Must
**Релиз:** v1.2
**Тип:** Контентная задача

### Описание
Наполнить раздел «Никнеймы и аватары» начальными данными для генерации никнеймов. Данные можно добавить через админ-панель или через seed-скрипт.

### Прилагательные (20 штук)

| # | textRu | textEn | isActive |
|---|--------|--------|----------|
| 1 | Быстрый | Swift | true |
| 2 | Храбрый | Brave | true |
| 3 | Мудрый | Wise | true |
| 4 | Хитрый | Cunning | true |
| 5 | Весёлый | Cheerful | true |
| 6 | Тихий | Quiet | true |
| 7 | Яркий | Bright | true |
| 8 | Ловкий | Agile | true |
| 9 | Дерзкий | Bold | true |
| 10 | Сонный | Sleepy | true |
| 11 | Гордый | Proud | true |
| 12 | Шустрый | Nimble | true |
| 13 | Дикий | Wild | true |
| 14 | Ночной | Nocturnal | true |
| 15 | Полярный | Polar | true |
| 16 | Огненный | Fiery | true |
| 17 | Звёздный | Stellar | true |
| 18 | Тайный | Secret | true |
| 19 | Лунный | Lunar | true |
| 20 | Грозный | Mighty | true |

### Животные (20 штук)

| # | textRu | textEn | emoji | isActive |
|---|--------|--------|-------|----------|
| 1 | Лис | Fox | 🦊 | true |
| 2 | Волк | Wolf | 🐺 | true |
| 3 | Медведь | Bear | 🐻 | true |
| 4 | Сова | Owl | 🦉 | true |
| 5 | Орёл | Eagle | 🦅 | true |
| 6 | Тигр | Tiger | 🐯 | true |
| 7 | Лев | Lion | 🦁 | true |
| 8 | Панда | Panda | 🐼 | true |
| 9 | Кот | Cat | 🐱 | true |
| 10 | Пёс | Dog | 🐶 | true |
| 11 | Дельфин | Dolphin | 🐬 | true |
| 12 | Пингвин | Penguin | 🐧 | true |
| 13 | Коала | Koala | 🐨 | true |
| 14 | Единорог | Unicorn | 🦄 | true |
| 15 | Дракон | Dragon | 🐉 | true |
| 16 | Кролик | Rabbit | 🐰 | true |
| 17 | Ёж | Hedgehog | 🦔 | true |
| 18 | Хамелеон | Chameleon | 🦎 | true |
| 19 | Фламинго | Flamingo | 🦩 | true |
| 20 | Осьминог | Octopus | 🐙 | true |

### Критерии приёмки
- AC-1: В разделе «Прилагательные» — 20 записей, все активны
- AC-2: В разделе «Животные» — 20 записей, все активны, у каждого есть эмоджи
- AC-3: Генерация никнеймов работает (комбинация «Прилагательное Животное»)

---

## Порядок выполнения

```
BUG-302 (модалки)     ─┐
BUG-303 (справочник)   ├─→ могут выполняться параллельно
BUG-301 (иконки)       │
FR-210  (скрыть AI)   ─┘
         │
         ▼
FR-214 (переименовать) ─→ FR-215 (заполнить данные)
FR-211 (поиск/фильтры) ─→ FR-213 (создать подборки) ─→ BUG-304 (мобильные подборки)
FR-212 (emoji-пикер)
```

**Первая очередь (параллельно):** BUG-301, BUG-302, BUG-303, FR-210
**Вторая очередь:** FR-211, FR-212, FR-214
**Третья очередь:** FR-213, FR-215
**Четвёртая очередь:** BUG-304 (верификация после создания подборок)

---

## Требования к тестированию (Playwright E2E)

После выполнения всех задач необходимо провести полное регрессионное тестирование админ-панели с помощью Playwright.

### Тестовые сценарии

**Группа 1: Навигация и общий интерфейс**
- [ ] Все пункты меню кликабельны и ведут на корректные страницы
- [ ] Пункт «AI Генерация» отсутствует в навигации
- [ ] Пункт «Никнеймы и аватары» присутствует вместо «Справочники»
- [ ] Страница `/questions/generate` доступна по прямой ссылке

**Группа 2: Модальные окна**
- [ ] Модалки открываются по центру экрана на всех страницах
- [ ] Backdrop затемняет экран
- [ ] Клик по backdrop закрывает модалку
- [ ] Кнопка «X» закрывает модалку
- [ ] Проверить на: Категории, Никнеймы (3 вкладки), Подборки

**Группа 3: Категории**
- [ ] Иконки отображаются как эмоджи, а не текст
- [ ] Создание категории с эмоджи через emoji-пикер
- [ ] Редактирование категории
- [ ] Удаление категории

**Группа 4: Ежедневные наборы**
- [ ] Страница создания набора загружается без ошибок
- [ ] Поиск по тексту утверждений работает
- [ ] Фильтр факт/фейк работает
- [ ] Фильтр по категориям работает
- [ ] Комбинация фильтров работает
- [ ] Выбор 10 утверждений и создание набора

**Группа 5: Никнеймы и аватары**
- [ ] Страница загружается без ошибок
- [ ] Все 3 вкладки переключаются корректно
- [ ] Прилагательные: CRUD + toggle статуса
- [ ] Животные: CRUD + toggle + эмоджи отображаются
- [ ] Эмоджи: CRUD + toggle
- [ ] Содержит 20 прилагательных и 20 животных

**Группа 6: Подборки**
- [ ] Минимум 2 подборки в списке
- [ ] Создание подборки с вопросами
- [ ] Публикация подборки
- [ ] Emoji-пикер работает в форме

**Группа 7: Различные viewport'ы**
- [ ] Desktop (1920x1080)
- [ ] Laptop (1366x768)
- [ ] Tablet (1024x768)

### Конфигурации Playwright
```typescript
const viewports = [
  { name: 'Desktop', width: 1920, height: 1080 },
  { name: 'Laptop', width: 1366, height: 768 },
  { name: 'Tablet', width: 1024, height: 768 },
];

const browsers = ['chromium', 'firefox', 'webkit'];
```

При обнаружении ошибки — создать баг-репорт с описанием, скриншотом и шагами воспроизведения, и передать разработчику на исправление.
