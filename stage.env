VITE_API_URL=https://6804903-vp245627.twc1.net/api
VITE_ENV=stage

# PostgreSQL (используется контейнером postgres)
POSTGRES_USER=factfront
POSTGRES_PASSWORD=factfront_stage
POSTGRES_DB=factfront_stage

# Database (DATABASE_URL переопределяется в docker-compose через имя сервиса postgres)
DATABASE_URL="postgresql://factfront:factfront_stage@postgres:5432/factfront_stage"

# Server
PORT=3001
NODE_ENV=stage

# Auth
JWT_SECRET=stage-secret-factfront-change-me
JWT_EXPIRATION=1d
JWT_REFRESH_EXPIRATION=30d

# Admin
ADMIN_EMAIL=admin@factfront.app
ADMIN_PASSWORD=admin123stage

# AI — LLM
ANTHROPIC_API_KEY=sk-ant-placeholder
AI_LLM_PROVIDER=anthropic
AI_LLM_MODEL=claude-sonnet-4-6

# AI — Images
OPENAI_API_KEY=sk-placeholder
AI_IMAGE_PROVIDER=dall-e
AI_IMAGE_MODEL=dall-e-3

# S3 (Cloudflare R2)
S3_ENDPOINT=https://placeholder.r2.cloudflarestorage.com
S3_REGION=auto
S3_BUCKET=factfront-images-stage
S3_ACCESS_KEY_ID=placeholder
S3_SECRET_ACCESS_KEY=placeholder
S3_PUBLIC_URL=https://images-stage.factfront.app

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=1000

# CORS
CORS_ORIGIN=https://6804903-vp245627.twc1.net
